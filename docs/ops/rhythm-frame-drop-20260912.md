# モンヒロビートのカクつき: 切り分けの記録(2026-09-12)

ユーザー報告。

> 「ノーツが落ちてくるのが今までより少しかくついて見える。ノーツ側に何かありそう」
> 「モンスターノーツ踏んだときは明らかに固まったり飛ぶ」「毎回じゃなくてたまに」

**まだ直していない。** 原因の性質までは確定したが、最後の一点(何の描画が重いか)は
実機でしか測れないので、総当たりできる逃げ道を用意したところ。

## 確定したこと

### ① JavaScript は無罪

実機(iPhone・4G・1曲163秒)で、33msを超えたフレーム40件をそのまま控えた結果:

```
2.3s 47ms tick 0ms 遅れ 0ms 走査2/描画2 モンスター後 —
4.0s 37ms tick 1ms 遅れ 0ms 走査3/描画3 モンスター後 —
   …40件すべて tick 0〜1ms・遅れ 0ms・走査1〜9個
```

**飛んだフレームでJSはほとんど何もしていない。** 原因は描画側(GPUの合成・
ラスタライズ)かブラウザの外。

### ② 正体は「1.3秒に1回、1フレームだけ落ちる」

飛びはすべて34〜47ms。ほぼ「2フレーム分」＝1フレーム落ち。数字も合う。

```
平均59.2fps        → 1秒あたり0.8フレーム落ち
33ms超 124回/163秒 → 0.76回/秒
```

ドカッと固まるのではなく、**描画が16.67msにほんのわずか間に合っていない**。
これが「ノーツが少しカクつく」の正体。

### ③ モンスターノーツは「踏んだ直後」に4回中2回

```
30.4s  36ms  モンスター後   37ms   ← 踏んで2フレーム後
65.1s  49ms  モンスター後   50ms   ← 踏んで3フレーム後
```

**軽量モードでも起きる**ので、光や画面全体のフラッシュではない。
判定処理そのものも 1.29〜3.00ms と軽い(setView による React の再描画は
次のフレームで起きるので、この数字には入っていない)。

### ④ canvas の解像度は主因ではない

軽量モード(dpr 3→2・画素数44%減)で測っても、ほぼ変わらない。

| | 通常 | 軽量 |
| --- | --- | --- |
| 33ms超 | 124 | 96 (-23%) |
| 25ms超 | 448 | 528 (+18%) |
| 平均fps | 59.2 | 59.1 |

## 外れた仮説(測って否定したもの)

1. **Tailwind の静的化(#1326)** … 配信CSSとCDN相当でフレーム間隔も computed style も一致
2. **#1328 ライフとコンボの強調** … 測定のばらつきを差と読み違えた。撤回済み
3. **曲の時刻の粗さ** … 実機で「進まないframe 0.0%」。完璧に滑らかだった
4. **モンスターノーツの判定処理** … 1.29〜3.00ms。ふつうのノーツより軽い回もある
5. **ヒットエフェクトの発動(will-change が無い)** … 本番の関数で12回出して
   12回とも16.6〜16.7ms。1フレームぶんの遅れも出ない
6. **canvas の解像度** … 上の④

## この環境では測れないもの

**プレイエリアの装飾の合成コストは、ここでは判定できない。**
ヘッドレスのLinuxはGPU合成が実機のiPhoneと別物で、

- CPUを1/4に絞る → 全条件で30fpsに飽和して差が出ない
- 絞らない → 全条件で60fps・33ms超0回で差が出ない

という結果になった(`blur` あり/なし、`drop-shadow` あり/なし、層そのものを消す、
の5条件で比較)。

**あわせて、このサンドボックスでは曲が一度も鳴らない**
(`AudioContext.currentTime` の読み出し0回・`playing:[]`)。
ここでの実測は「無音で演奏した場合」の数字でしかない。

## いま用意してあるもの(実機で総当たりする)

デバッグ画面の**赤いパネル「装飾を切って切り分ける」**で、次の演奏から個別に消せる。

| トグル | 何を消すか |
| --- | --- |
| 上の光（blur 12px） | `[data-rhythm-play-area]::after` |
| 奥行きの格子（drop-shadow） | 同 `::before` |
| 背景のグラデーション3層 | 同 `background` と `inset box-shadow` |
| レーンの影と縁 | `[data-rhythm-lane]` の `filter`/`box-shadow`/`::before` |

**ONにして1曲プレイ → 性能計測の「33ms超」を見る。減ったものが原因。**
まず4つ全部ONで、装飾が原因かどうか自体を確かめるのが早い。

保存は新しいキー `mh_rhythm_strip_v1`。既存の音ゲー設定・BESTには触らない。

## 道具

```
node tools/mode/rhythm-live-frame-report.js --cpu 4 --runs 5 --draw
```

本番の index.html を開いて実際に演奏させ、フレーム間隔・スタイル再計算・
canvasへの描き込み回数・位置の測り直しを測る(報告だけ)。
`--compare` はCSSの切り分け、`--inject-css` は静的CSS以前のコミットを測るため。

> ⚠️ **この環境のフレーム数は同じコミットでも244〜308とばらつく(幅17%)。**
> 2〜3回の測定で「落ちた」と言わないこと(実際にそれで誤った結論を出した)。
> `--runs 5` 以上で中央値を見るか、そもそも「1フレームで何回呼ばれるか」を数える。

## ついでに見つけたもの(主因ではない)

```
先頭スキップ/frame  299.7
```

毎フレーム、終わったノーツを約300個ぶん読み飛ばしている。曲が進むほど増える作り。
`tick処理/frame 0.39ms` と軽いので急がないが、素直に直せる無駄ではある。

## 実装上の注意

`RHYTHM_STRIP` は `RHYTHM_PERF` の**後ろ**へ置くこと。あいだに入れると
`rhythm-perf-check.js` の切り出し(`const RHYTHM_PERF_KEY=` から最初の `})();` まで)が
そこで止まり、「RHYTHM_PERF is not defined」で落ちる(実際に踏んだ)。

> ⚠️ 音ゲーの描画は別の会話でも触っている。**手を入れる前に担当を決めること。**

## 直したもの(2026-09-12 19:54)

**コンボ枠の脈動が、軽量モードでも演出量MINIMALでも止まっていなかった。**

`index.html` の演奏画面CSSには2026-09-07から

> 脈動は `opacity` だけ。`box-shadow` をキーフレームで変えると、モンスターノーツが
> 見えているあいだ毎フレームぼかしを描き直す

と書いてあるのに、**守れているかを見る検査が無かった**。唯一の違反が
`mhRhythmComboGlow`(ぼかし 2px→12px)と `mhRhythmComboGlowHot`(3px→14px→24px)で、
しかも HUD は `[data-rhythm-play-area]` の**外の兄弟**なので、
プレイエリア配下へ書いた軽量モードの打ち消しが1つも届いていなかった。

- 演奏画面の根(`<main data-rhythm-tap-test>`)へ `data-rhythm-lightweight` /
  `data-rhythm-effect` を出し、その目印で HUD 側も止めた。**通常モードの見た目は変えていない**
- 見張りを新設: `node tools/mode/rhythm-keyframe-blur-check.js`

> **これは主因ではない。** コンボ枠は 62.6×41.0px(canvas の1.7%)で、200コンボ以降しか
> 走らず MISS で 0 に戻る。「曲の序盤から1.3秒に1回、1フレームだけ落ちる」という症状と形が合わない。
> 直したのは「ルール違反」と「軽量モードの設定の抜け」の2つ。

## 原因が1つ見つかった(2026-09-12 20:55)— CSSを読むだけで分かった

ユーザー指摘。

> その4つだけでかくつきが起きるってのがそもそもピンとこないんだけど。大した演出じゃないよね？
> 演出の出し方に問題ある方を疑うんだけどそういう発想ある？
> ここまでおおがかりな調査が必要な案件とは思えない

**そのとおりだった。** 「装飾を切ったら半分になった」から言えるのは
「**その装飾が毎フレーム塗り直されている**」であって「装飾が重い」ではない。
あれは全部**静止している絵**で、本来は1度描けば使い回せる。
毎フレーム作り直されているなら、**出し方のほうがおかしい**。

読み直したらすぐ出てきた。

```css
[data-rhythm-lane]::after {
  filter: blur(4px);
  transition: opacity 60ms linear, filter 60ms linear;   /* ← ぼかし半径を補間している */
}
[data-rhythm-lane][data-pressed="true"]::after {
  filter: blur(1px);                                      /* ← 押すと 4px → 1px */
}
[data-rhythm-lane][data-pressed="true"] { filter:brightness(1.15) drop-shadow(1px 0 …); }
```

**ノーツを1回叩くたびに、そのレーンで filter のトランジションが2本、60msかけて走る。**
曲中は絶え間なく叩くので、**常にどこかのレーンでぼかしが作り直され続けている**。

これは `index.html` に自分で書いてある禁止事項そのものだった。

> 脈動は `opacity` だけ。`box-shadow` をキーフレームで変えると、毎フレームぼかしを描き直す

同じ日に作った `rhythm-keyframe-blur-check.js` が `@keyframes` しか見ておらず、
**`transition` で同じことをしている箇所を丸ごと見逃していた**。検査の穴。

### 直したもの(見た目は1画素も変わらない)

| 場所 | 前 | 後 |
| --- | --- | --- |
| `[data-rhythm-lane]::after` | `transition:opacity 60ms, filter 60ms` | **`transition:opacity 60ms`**(filter は切り替わる瞬間の1フレームだけ) |
| レーンの `transition`(JSX) | `background-color, box-shadow, filter, border-color` | **filter を外す** |
| サブレーンの光10枚 | `willChange:'opacity'` を常時 | **外す**(押していないあいだも10枚が居座っていた) |

`filter` そのものは残してある。**静止している filter は1度描けば使い回せる**ので、
問題なのは「変わること」のほう。だから `transition` から外すだけで足りる。
画素比較(`rhythm-decor-pixel-check.js`、押したレーンも含む)で**最大差0・平均差0**を確認済み。

### 検査を広げた

`rhythm-keyframe-blur-check.js` に2つ足した。

- ③ 演奏画面の `transition` に `filter` を入れていない(CSSとJSXの両方)
- ④ 演奏画面で `will-change` を条件なしに付けていない

どちらも**わざと元の書き方へ戻して落ちること**を確かめてある(空振りしていない)。
③はセレクタの拾い方を一度まちがえて「演奏画面の外」と誤って通したので、
コメントを長さごと空白へ潰してから拾う形に直した。

### まだ分かっていないこと

これで**どれだけ減るかは実機で測るまで分からない**。
装飾4つを切ったときの半減(25ms超 448→214)のうち、どれだけがこれだったのか。
残りが大きいなら、次に疑うのは同じ形のもの
(`::before` の `clip-path`＋`filter` の併用、背景の `box-shadow:inset 0 -42px 80px`)。
どれも「静止しているのにキャッシュされていない」という同じ話なので、
測る道具を増やすより**CSSを読んで直す**ほうが早い。

## 事前準備でどこまで消えるか（2026-09-12 21:39）

ユーザーの問い「演奏前に事前ダウンロードみたいな機能をいれて終わってから演奏開始とか意味ない？」
「そんぐらいならそもそもダウンロード時間もあってないようなもの？」への答え。

### ダウンロードは0バイト

| もの | いつ用意しているか |
| --- | --- |
| 曲の音源 | `startRhythmTrack(...,{autoStart:false})` を **await してから**開始 |
| 曲えらびのジャケット | 曲えらびを開いたときに読み終わっている |
| 譜面 | 起動時（`index.html` の `SIZES`） |
| ヒットエフェクトの器10個 | `beginRun` で先に作る（`rhythmEnsureHitEffects`） |
| タップ音 | 最初のタッチで `warm()` |

**読み込み画面を足しても出すものが無い。** 残っていたのは通信ではなく「描画の準備」だった。

### 残っていたのは光のスプライト

`glowSprite` / `auraSprite` / `arrowSprite` は id でキャッシュしており、
**その種類のノーツが曲の中で初めて出た瞬間**に作っていた。
モンスターノーツは3枚（glow + 外アウラ + 内アウラ）をまとめて作るのでいちばん重い。

`node tools/mode/rhythm-canvas-note-cost.js` で測った枚数（時間は機械の調子で揺れるので枚数で見る）。

| | 曲の中で作られる枚数 |
| --- | --- |
| 先焼きなし | **11枚** |
| 先焼きあり | **0枚** |

先焼きの11枚が、描くのに必要な11枚とぴったり一致する（検査が見張っている）。

### 直したもの

`RHYTHM_CANVAS_RENDERER.warmSprites(options)` を足し、`beginRun` で
`rhythmEnsureHitEffects` の隣から呼ぶ。**READY→3→2→1 のカウントダウンが3.2秒あるので、
そこへ黙って寄せればプレイヤーには見えない。画面もUIも増やしていない。**

⚠️ 渡す値は実際に描くときと同じでなければならない。スプライトのキャッシュのキーは
「種類と画素密度」だけなので、違う色・太さで焼くとそのまま曲の終わりまで使われる。
画素密度も `begin()` と同じやり方で決める（食い違うと最初の `begin()` が
`sprites.clear()` を呼び、焼いたぶんが丸ごと捨てられる）。

### これで消えるのは「初回だけ」

`③ モンスターノーツは「踏んだ直後」に4回中2回`（上の節）は、
**踏んで2〜3フレーム後**に落ちていて、判定処理そのものは 1.29〜3.00ms と軽い。
先焼きで消えるのは「その種類が初めて出たとき」の1回だけなので、
**毎回の落ちは別の原因**。次に疑うのは `setView` による React の再描画
（モンスターノーツだけ `view.ability` が増えて能力の帯がDOMへ挿し込まれる。
軽量モードでも帯自体は出る＝影だけ消えるので、「軽量モードでも起きる」と符合する）。

## 原因が見つかって直した(2026-09-25)

ユーザー報告「どの端末でも演奏中のカクつきの報告がある / モンスターノーツを踏むと画面が飛ぶ /
譜面が多いと起こりやすい / プロセカやバンドリと比べてそんな重くないはず」。

**上の「JSは無罪」は tick の中だけを見ていた。** tick の外で、タップのたびに別の重い処理が走っていた。
測り方は「譜面どおりに自動で叩く」(pointerType は `touch` 以外。`pointerDown` は touch を受けない)＋
CPUプロファイルで重い瞬間だけ抜き出す＋React の開発用フックで「どのコンポーネントのどの state が変わったか」を記録。

| 原因 | 何が起きていたか | 直し方 |
| --- | --- | --- |
| **タップの波紋が App の state** | `ripples` を `MonsterHeroGame` の state に積んでいたので、**タップのたびにゲーム全体が2回描き直され**、演奏画面も作り直されていた。10秒の演奏で React の描き直し58回のうち大半がこれ | 波紋は入れ物(`data-mh-tap-ripples`)へ要素を直接足して650ms後に外す。見た目は同じ |
| **モンスターノーツの顔が DOM** | 元絵(最大1024px)＋部位ごとの染め絵をマスクで重ね、影を2重に付けた要素を、合成レイヤーの**内側**で毎フレーム拡大していた。顔が見えている4秒で画像のデコード259回 | 演奏前に1枚の小さな絵へ焼き(`bakeDyedMonsterCanvas` / `rhythmBakeMonsterFace`)、ノーツの canvas へ `drawFace` で描く。デコード0回・ラスタ処理 105→48回 |
| **両サイドのマスモンも同じ重ね絵** | 能力で1.14倍になるあいだ、マスクを掛け直して描き直す | 焼いた絵を `<img>` で出す(焼けるまでは従来の重ね絵) |
| **流し直しの `offsetWidth`** | 判定のたびにレイアウトを確定させていた | `getComputedStyle(...).animationName`(スタイルだけ)に替えた。演出の再生回数は変更前と完全に一致 |
| ~~入力の箱の測り直し~~ | ~~tick の冒頭で測っておく~~ | **2026-09-26にやめた**(下の節) |
| 経過時間のバー | 0.1%刻み(1秒に約7回)で書き換え、そのたびに画面全体の層を塗り直し | 0.5%刻み |
| 引き継ぎ画面の見張り | DOM が変わるたびに rAF を積み、見出しを全部読んでいた | 予約を1つにまとめ、演奏中は探さない |

**この環境(GPUなし)のフレーム間隔では差が出にくい**(canvas を毎フレームCPUから送る Commit が
50〜60ms 出るため)。効き目は「描き直しの回数・画像のデコード回数・JSの時間」で見ること。
CPUを1/4に絞って叩いた10秒で、JS 1,120ms → 940ms、25ms超のフレーム 82 → 70(3回ずつの平均)。

## 演出量を上げたときだけ重い(2026-09-26)

ユーザー報告「設定で軽くしてると平気だけど、演出量を上げるとやっぱり重くてカクつく(iPhone 16e)」。

**原因は判定文字の光。** `filter:drop-shadow` を最大6枚(いちばん外はぼかし40px)重ねていて、
「多め」「最大」では判定のたびに弾ませる(`mhRhythmJudgmentPop`)。弾むあいだだけ合成レイヤーが作られ、
そのたびに**文字の何倍もの大きさ(1010×696px)のレイヤーをぼかし直していた**。
レイヤーを固定しても、合成のたびにぼかしを掛け直すのでフレーム数は戻らない(213回/6秒)。

| 6秒(自動演奏・CPU等倍) | 描画のフレーム | ラスタ処理 |
| --- | --- | --- |
| 最大(直す前) | 180〜208回 | 6,157〜7,140ms |
| 標準(直す前) | 354〜361回 | 180〜296ms |
| 最大(直した後) | **361回** | **120ms** |

**直し方**: 光(影の重なり)だけを判定の種類ごとに canvas で1度焼き(`rhythmBakeJudgmentHalos`)、
`::before` に置く。字は `::after` に同じグラデーションで重ね直し、模様の位置は `background-position:inherit` で
本体の流れ(金・虹)を受け継ぐ。本体の `filter` は外す。
- 影の値は画面に当たっている CSS(`getComputedStyle(...).filter`)から読むので、演出量ごとの違いもそのまま。
- canvas の `shadowBlur` = CSS の drop-shadow の2倍。filter の並びと同じ順に「それまでの全部の影」を下へ足す。
- 画面の文字幅と canvas の文字幅が2px以上ずれたら焼かない(フォントが違う)。影が1枚だけの判定も焼かない。
- 画素比較(演奏の状態に影響されない場所で撮る)で、どの判定も平均差1.3以下(0〜255)。

⚠️ **画素比較は演奏画面の外で撮ること。** 叩かずに流すとライフが0になり、演奏画面の外の暗幕(DOWN)が
重なって、片方だけ暗く写る(それを「差」と読み違えかけた)。

⚠️ ふつうの MARVELOUS の金の流れは、「多め」「最大」では弾みのアニメーション(`animation` の書き換え)に
上書きされて止まっている(ぴったりの虹は詳細度が高いので流れる)。今回の直しはこの動きも変えていない。

## ライブ背景「派手」の発熱(2026-09-26)

ユーザー報告「演出量とライブ背景をマックスに上げると重くなるし発熱がすごい」。

**試してやめたこと**: 背景(ジャケット・暗幕・サーチライト・光の粒)を1枚の canvas へ毎フレーム描く。
合成だけで済んでいた動きが毎フレームの塗りになり、この環境の計測で処理時間が 5.0秒 → 7.9秒(8秒・CPU 1/4)に増えた。
動くものの描き直しを1.5倍の画素密度の別 canvas に分けても同じ。iPhone の Safari も 2D canvas は CPU で塗るので、同じ向きに効くはず。
さらに、描き直さない canvas の要素を並べても、この環境では合成のたびの余分な仕事(Commit)が増えた。

**入れたもの**: 動きは CSS のアニメーション(合成だけ)のまま、合成する中身だけを軽くした。
- サーチライト … clip-path の切り抜き(回転する層のマスク)をやめ、台形を焼いた画像(`<img>`)にした。色の段ごとに4枚
- 光の粒 … CSS の模様(radial-gradient)で作った「画面の2倍の高さ・画面の画素数そのまま」の層を、1.5倍までの画素密度で焼いた画像にした
- ジャケット(24×24 の引き伸ばし)と暗幕(動かない層)はもともと軽いので変えていない

⚠️ **この環境には GPU が無いので、合成の軽さ(マスク・絵の大きさ)は数字に出ない。** フレーム数・処理時間は変更前と同程度(ばらつきの範囲)。
発熱の効き目は実機で見るしかない。アニメーションを止めた状態の画素比較で、光の形・位置は変更前と一致(違いはカウントダウンの数字だけ)。

## 入力の箱を tick の冒頭で測る形をやめた(2026-09-26)

9/25 に「タップのたびの強制レイアウトを減らす」ため、tick の冒頭でプレイエリアの箱を測っておく形にした。
ところが毎フレームの最初の時点で配置の計算がたまっていることが多く、**毎フレーム**その場で計算させていた。
CPUプロファイル(8秒・CPU 1/4・自動演奏)で `getBoundingClientRect` が全体の8.6%・560ms(タップのときだけ測る以前の形は10秒で66ms)。
元の「タップが来たときに初めて測る」形へ戻した。**「冒頭で測れば安い」は、配置が毎フレーム崩れている画面では成り立たない。**

## 2026-09-26 WebGL(検証用)のときは、叩いたときの光も canvas で描く

デバッグ画面「ノーツの描き方(検証用)」で WebGL を選んだときだけ、叩いたときの光(芯・柱・火花5つ・十字の光)を
DOM の部品ではなくノーツの canvas へ描く。2D の canvas と DOM 版は今までどおり部品のまま。

- 形・動き・色は CSS(`[data-rhythm-hit-effect]` と `@keyframes mhRhythmHit*`)を写した。明るさのフィルターは部品に印を付けて実際の値を読む
- 部品と canvas を同じ経過時間で止めて比べ、大きく違う画素は0.1%以下(時刻・演出量・幅・モンスターノーツ・ぴったりの虹)
- 実測(monster_hero HARD・演出最大・ライブ背景 VIVID・CPU 1/4・6秒): style 981〜1,065ms → 664〜711ms、UpdateLayoutTree 254〜261ms → 197〜205ms、光の CSS アニメーション 320本 → 0本。JavaScript の時間は変わらず
- CSS の動きを直したら canvas 側(HIT_KEYS)も直す。`tools/mode/rhythm-hit-effect-check.js` が数字のずれを見張る

## 2026-09-26 WebGL(検証用)の描き込み先の JavaScript を軽くした

WebGL のときは、通常の canvas より JavaScript が多かった(CPU プロファイル 10秒・CPU 1/4・演出最大: `rhythm-mode.js` 971ms 対 441ms)。
上位は形を三角形に分ける処理(`tri` `disc` `triangulate` `cross`)。次の4つで、描く絵は変えずに軽くした(2D との画素の差 0.471 → 0.470)。

- 三角形を足すたびに作業用の配列を作っていた → じかに書き込む
- 凸形でも毎回「耳を切る」方式で分けていた → 凸形は1点から扇に分ける(ノーツの形はほとんど凸形)
- 丸を描くたびに sin・cos を計算していた → 分け方ごとに一度だけ計算して使い回す
- 塗るたびに GPU の入れ物を作り直していた → 使い回す(`bufferSubData`)
- ついでに、入れ物を大きくするときにそれまでの三角形を捨てていた不具合を直した(点の多い形で前半が消える)

結果: `tick` の合計 1,304ms → 約865ms、`rhythm-mode.js` 971ms → 約608ms(通常の canvas の `tick` 947ms より少ない)。

ノーツの描き方に WebGL の描き込み先が知らない命令を足すと、WebGL のときだけその部分が出なくなる。
`tools/mode/rhythm-webgl-notes-check.js` が、同じノーツの並びを 2D と WebGL で描いて画素を比べて見張る。

## 2026-09-26 描画方式の既定を「自動」にした(WebGL を既定へ)

- 「自動」は、見えない試し用の canvas で1回だけ GPU を調べ(`rhythmWebglGpuUsable`)、ちゃんとした GPU があれば WebGL、
  ブラウザが CPU で肩代わりしている(SwiftShader・llvmpipe など)なら Canvas で描く。本番の canvas で調べないのは、
  一度 WebGL を作った canvas からは 2D を取り出せないため
- 保存済みの STANDARD(Canvas)・LIGHT(WebGL)は意味を変えずにそのまま(移行はしない)。既定値だけ AUTO にした
- 曲を抜けるたびに WebGL を片付ける(`WEBGL_lose_context`)。iPhone などは同時に持てる数に上限がある
- 焼いた絵は演奏の前に GPU へ渡しておく(`preloadImage`)。初めて出た瞬間の引っかかりを防ぐ
- テスト環境(GPU なし)では「自動」は Canvas になる。GPU がある端末に見せかけると WebGL になることを確かめた

## 2026-09-26 WebGL へ送る命令を減らした(1フレーム 約455回 → 約209回)

スマホのブラウザは、WebGL の命令1回ごとの手間が大きい。描く回数(約40回/フレーム)は変えずに、
同じ値を毎回送り直していたもの(位置の変換・画面の大きさ・透明度・塗り方・色・貼る絵・入れ物の指定)を、変わったときだけ送るようにした。
ステンシル(1画素1度)は、線と、形が2つ以上重なる塗り・重なりうる分け方をした塗りのときだけ使う(絵の貼り付け・1つの凸形の塗り・四角は三角形が重ならないので要らない)。
2D との画素の差は 0.470 のまま。設定画面の「描画方式」の下に、この端末がどちらで描くか(「自動」の結果)を出すようにした。

## 2026-09-26 WebGL: 光の足し算と、描く回数のまとめ

- 光(粒の光・オーラ・終わりの横棒の光・押さえ中の光・叩いた光)だけ、WebGL のときは足し算で重ねる(`globalCompositeOperation='lighter'`、
  色だけ足して透明さはふつう)。Canvas では重い(60→30fps)ので今までどおり。`RHYTHM_CANVAS_RENDERER.additiveGlow` で切り替えられる
- 続けて同じ状態で描くもの(同じ絵・同じ色・ステンシル不要)は、GPU へ頼むのを待って1回にまとめる(`flush` → 待ち / `issue` で実際に描く)。
  待ちはフレームの終わり(`end()`)・状態が変わる前・一部を消す前・絵を捨てる前・マイクロタスクで必ず描き切る
- 1フレームの描画 39.9回 → 32.8回。入れ物は毎回新しく用意する(bufferData)ままにした(上書きの使い回しは iPhone で残像になった)

## 2026-09-26 WebGL: にじむ光(ブルーム)をオプションに足した(既定は OFF)

- オプション → システム →「にじむ光」(`noteBloom`、既定 `false`)。描画方式が WebGL のときだけ効き、エフェクトが「最小」・軽量表示のときは切る
- 足し算で描いた光(`lighter`)だけを画面の 1/4 の FBO へ描き直し、9点ガウスで横・縦にぼかして足し算で重ねる(`bloomFinish`)。
  強さは gain .45 / 広がり 2.6(最初の .85 / 1.6 は白く飛びすぎた)
- GPU への命令は 1フレーム 約201回 → 約269回。重さが増えるので既定では切っておく

## 2026-09-26 WebGL: ライブ背景を canvas 1枚に描く

- 描画方式が WebGL のとき(ノーツが WebGL で描かれるとき)は、ライブ背景(ジャケット・暗幕・サーチライト・光の粒)を
  WebGL の canvas 1枚へ、全画面1回の描画でまとめて描く(`rhythmCreateStageGL`)。動きがゆっくりなので描き直しは1秒に30回まで
- CSS 版は「ジャケット・暗幕・サーチライト2枚・画面の2倍の高さの粒の層2枚」を毎フレーム合成していて、GPU は画面およそ7枚ぶんを塗っていた。
  WebGL 版は画面1枚ぶん(画素密度は CSS 版の焼いた画像と同じ1.5倍まで)
- ノーツのタイミングの光(`[data-rhythm-stage-pulse]`)はレーンの上に置く必要があるので CSS 版のまま
- 同じ時刻で止めて比べると、大きく違う画素は 0.11%(HUD の時計の数字だけ)。WebGL を失うと CSS 版へ戻る
- デバッグ画面「ライブ背景の描き方(検証用)」(`mh_rhythm_stage_gl_v1`: 自動 / CSS / WebGL)で、実機の重さ・発熱を比べられる
- ⚠️ この環境の WebGL は SwiftShader(CPU で肩代わり)なので、重さの比較は実機でしか意味がない

## 2026-09-27 道の演出(オプション・既定 OFF)

- 拍の線(奥から流れる。小節の頭は明るい線)と道のふちの光(拍を過ぎた瞬間に光る)は、ノーツの canvas へ `drawRoadFx` で描く。
  線は横まっすぐなので fillRect、ふちは道の境目と同じ投影で作った細い多角形。どちらも叩いたときの光・ノーツより下
- 奥のもや(`[data-rhythm-road-haze]`)と光(`[data-rhythm-road-glow]`)は動かない CSS の層。光だけ小節の頭で Web Animations の opacity を動かす
- 拍は `RHYTHM_SONG_BEATS`(解析ファイルの timing を写したもの)。公開中23曲は全難易度でノーツが格子に乗っている(`rhythm-song-beats-check.js`)
- 重さ: 描く部品だけを120フレーム描かせる計測(CPU 1/4)では、線・ふち・両方とも描かない場合と同じ 2.0秒で、差は見えない。
  演奏全体(CPU 1/4・6秒・Canvas)では、25ms を超えるフレームが ON で多めに出る傾向(ON 6〜21本、OFF 2〜6本。外れ値を除く)。
  フレーム数は ON 243〜349・OFF 330〜351 とぶれが大きい。もやの層・光の脈打ち・線・ふちを1つずつ外しても、どれか1つが原因とは言えなかった。
  この環境は GPU が無く合成も CPU なので、実機の重さは実機で見る(既定 OFF にしてあるのはこのため)

## 2026-09-27 ライブ背景「ライブ」(4つ目の段)

- 「派手」に加えて、拍に合わせて明るくなるサーチライト・小節ごとに向きと色が替わるレーザー4本・道の両側に地平線まで並ぶ観客のペンライト。
  すべて WebGL の背景1枚のシェーダの中で描く(`rhythmCreateStageGL` の `live`)。描き直しは今までどおり1秒に30回まで
- 拍は曲の時刻から数える。演奏の毎フレームの処理(tick)が `stageClockRef` へ時刻を書き、背景の描き直しがそこから拍と拍の頭の光を出す
- ノーツが Canvas でも、GPU がある端末(`rhythmWebglGpuUsable`)なら背景だけ WebGL で描く。GPU が無い端末では「派手」と同じ(CSS 版)
- ペンライトは最初、画面の下(客席の最前列)に置いたが、縦持ちでは道に隠れてほぼ見えなかった。背景が見えるのは道の左右の三角のすき間
  (上ほど広い)なので、道の両側に奥まで並べる形にした
- ⚠️ シェーダの関数の順番を間違えて読み込みに失敗し、黙って CSS 版へ戻っていた(見た目は「派手」のままなので気づきにくい)。
  `tools/mode/rhythm-stage-gl-check.js` がシェーダの読み込みと、「派手」「ライブ」「拍の頭」の描き分けを見張る

## 2026-09-27 判定の演出・ノーツの動き・コンボの節目(オプション・既定 OFF)

- 判定の演出(`judgmentFx`): GREAT 以上で `[data-rhythm-judgment-burst]` を弾ませ直す(十字の光の筋＋判定の色の光。ぴったりは虹の筋)。
  形と色は動かない背景の模様で、動かすのは transform と opacity だけ。文字の弾み(`mhRhythmJudgmentPopVivid`)は演出量「標準」でも出す
- ノーツの動き(`noteMotionFx`): 描く部品の `motion`。上フリックは焼いた矢印を3段に重ねて明るさの波を流す、横フリックは山形の残像、
  SLIDE は区切りごとに縦のグラデーションで光の波(明るさは画面の高さで決めるので継ぎ目で段にならない。区切りの index で決めたら段になった)
- コンボの節目(`comboMilestoneFx`): 100のくぎりで `[data-rhythm-combo-milestone]` の ::before(光の輪)と ::after(文字)を弾ませ直す。
  文字の箱は幅180pxで、コンボ数が右寄り・左なら揃えを変える(中央揃えのままだと右端で画面の外へはみ出した)
