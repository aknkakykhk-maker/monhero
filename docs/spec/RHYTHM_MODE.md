# 音ゲーモード設計書

現行確認基準: 2026-09-02 main `b2b2a64ccc4ca5baa63c5a9e796db58a637e7d84`（PR #970 マージ後、画面構造・ノーツ速度・表示geometry修正の作業開始時点）。現行挙動は常に最新 `main` を正本とする。

この文書は、Monster Hero の音ゲーモードについて、現在実装済みの内容、ユーザーと確定した未実装仕様、暫定値、まだ確定していない項目を分離して残すための正式設計書。

## この文書の読み方

- **現行仕様**: 上記確認基準の最新 `main` と照合済みの実装事実。
- **確定・未実装**: ユーザーと方針は確定しているが、まだゲームへ実装していない仕様。
- **暫定**: 現時点で採用する作業値。実装前またはバランス確認時に変更し得る。
- **未確定**: 過去ログから復元できていない、またはまだユーザーと決めていない項目。推測で埋めない。
- **初期設計**: 当時の決定を残す履歴。現在仕様とは限らない。

音ゲーの仕様を変更するPRでは、本書の更新要否も確認し、仕様が変わる場合は原則として同じ変更単位で更新する。

現行挙動と数値は、必ず最新 `main` の以下を正本とする。

- `monster-hero/data/rhythm-mode.js`
- `monster-hero/data/rhythm-timing.js`
- `monster-hero/data/rhythm-lane-svg.js`
- `monster-hero/data/rhythm-geometry-calibration.js`
- `monster-hero/src/game-system.jsx`
- `monster-hero/index.html`
- `monster-hero/debug/rhythm-chart-authoring-ui.js`
- `monster-hero/debug/rhythm-chart-visual-editor.js`
- `monster-hero/debug/rhythm-hold-resize.js`
- `monster-hero/debug/rhythm-slide-path-editor.js`
- `monster-hero/debug/rhythm-section-loop.js`
- `monster-hero/debug/rhythm-invalid-placement.js`
- `monster-hero/debug/atsu-cup-theme-easy-formal-candidate-v1.json`
- `tools/mode/rhythm-mode-*-check.js`
- `tools/mode/rhythm-chart-*-check.js`
- `tools/mode/rhythm-easy-alignment-audit.js`
- `tools/mode/rhythm-easy-formal-candidate-check.js`

この文書とコードが食い違う場合、**現在実装されている挙動については最新 `main` のコードを優先**し、その差異を本書へ反映する。ただし、ユーザーが現在の会話で新しく確定した未実装仕様は将来仕様として本書へ残す。

---

## 1. 初期設計の出典（履歴）

音ゲーの基礎設計は PR #862 `feat: add rhythm mode foundation (音ゲー基盤 STEP 1)` に残っている。

初期設計で確定していた骨格は以下。

- 通常公開はせず、デバッグ画面からのみアクセスする
- 5レーン
- ノーツは `TAP / HOLD / FLICK / SLIDE` の4種類
- 難易度は `EASY / NORMAL / HARD / EXPERT / MASTER` の5段階
- 各難易度に最大スコアを持たせる
- 判定は6段階
- スコアは「判定90% + コンボ10%」を基本構成とする
- 曲データは既存BGM登録を正本とし、音ゲー側は `trackId` で参照する
- 設定保存とBEST保存を独立キーで持つ
- 欠損・不正な保存値は読み込み時に正規化する
- 通常HOMEには入口を追加しない
- デバッグ画面で曲、難易度、最大スコア、設定、BESTを確認できるようにする

---

## 2. 基本構成（現行仕様）

### レーン

- 表示は5メインレーン
- 細分配置・入力の基準は10サブレーン
- `TAP` / `HOLD` / `FLICK` は `subLane`（始点0〜9）と `subLaneWidth`（幅1〜4）を持てる
- `subLane` を持たない従来ノーツは、メインレーン1本＝サブレーン幅2相当として後方互換を維持する
- 可変幅TAP/HOLD/FLICKは描画範囲 `subLane` 〜 `subLane + subLaneWidth` と同じprojectionを逆変換して開始入力を判定する
- 幅1の入力だけ、表示を太くせずサブレーン幅0.18ぶんを左右へ許容する。隣接時は中心に近い1ノーツだけをclaimedする
- 通常の1入力は1ノーツのみ取得し、別の指はそれぞれ独立して同時取得できる
- TouchではTAPに限り `radiusX` を使って指腹の接触範囲を補助入力へ反映する
- PR #939以降は「1本指最大3サブレーン」という固定上限を正本にしない。実際の接触範囲を70%へ縮小し、隣接サブレーンへ25%以上重なった場合だけTAP候補へ含める
- 現行の接触幅主要定数は `RHYTHM_TOUCH_RADIUS_SCALE=.70`、`RHYTHM_TOUCH_MIN_SUBLANE_COVERAGE=.25`、異常値guardはプレイエリア幅の `.25`
- `radiusX` が0・不正・未対応、または異常に大きい場合は中心入力へfallbackする
- 接触幅で追加する候補はTAP専用。HOLD / SLIDE / FLICKは1本指につき1操作を維持する
- 同一の幅2〜4 TAPを複数の接触サブレーンが覆っても1ノーツとして1回だけ判定する
- touchmoveでは直前の接触集合と比較し、新しく接触したサブレーンだけをTAPの新規入力候補にする。一度外れてから再び入ったサブレーンは再度候補にできる
- PR #941以降、押しっぱなし中の小さな中心揺れを移動扱いしない。中心deadzoneは6〜10pxの範囲を基本とし、プレイエリア幅2%を参照する
- 押しっぱなし中に指腹を明確に広げた場合のみ追加接触を許可する。拡張判定の基準は3px以上かつ10%以上
- 可変幅HOLDは開始ノーツ・HOLD帯・ENDバーを同じサブレーン範囲と幅で投影する
- `subLane` を持たない旧HOLDは、従来どおりメインレーン1本＝サブレーン幅2相当の見た目と入力を維持する
- 可変幅FLICKは開始ノーツと開始入力を同じサブレーン範囲・幅で扱い、ジェスチャー成立後にruntime上でHOLDへ変換されても元のFLICK幅を維持する
- `subLane` を持たない旧FLICKは、従来どおりメインレーン1本＝サブレーン幅2相当の見た目と開始入力を維持する
- STEP2B-1以降のSLIDEは、始点 `lane` と authored `slidePoints[].lane` / `endLane` を `0, 0.5, 1, 1.5 ... 4` の0.5レーン刻みで指定できる
- SLIDEの途中補間は連続座標のまま描画・追従判定し、0.5刻みに丸め直さない
- SLIDEは `subLaneWidth`（幅1〜4）を持てる
- `slidePoints[].subLaneWidth`（幅1〜4）で途中幅を指定できる
- 実効幅は point → note → 幅2 の順にfallbackし、point間は位置と同じ時間軸で連続補間する
- SLIDEの幅が無い、または不正な場合は従来どおり幅2相当として旧譜面を維持する
- 可変幅SLIDEは開始ノーツと操作中の頭をその時刻の幅へ追従させ、SLIDE帯の各segment両端へ異なる幅を投影する。ENDバーは最終pointの実効幅を使う
- SLIDE開始入力も同じ幅へ合わせる。幅1のみ既存のサブレーン0.18ぶんの入力余白を付ける
- SLIDE途中追従の許容は幅に合わせて変化するが、幅2は従来の `0.82` レーンを維持する
- スマートフォン縦画面・タッチ操作を主対象とする
- レーン、ノーツ、判定ライン、入力座標は同一のprojectionモデルを使う
- 現在は遠近表現を持ち、奥ほど狭く、判定ライン側ほど広く表示する
- 遠近感・収束率・ノーツ幅などの具体値は `monster-hero/data/rhythm-mode.js` を正本とする
- **収束率 `RHYTHM_PROJECTION_TOP_SCALE` は `.18`**（2026-09-03に `.30` から変更）。
  Safe Areaの二重掛けを直した結果プレイエリアの高さが 515px → 743px（iPhone 390×844相当、約1.44倍）になり、
  収束率は「プレイエリアの高さに対する比率」なので、縦だけ伸びると円錐が寝て奥行きが薄く見えるようになった
  （レーン外枠の傾きが以前の約69%まで浅くなった）。`.18` にすると傾きが以前の約96%まで戻り、
  上端が細く収束する設計イメージにも近づく。ユーザー確認のうえ採用した値

### プレイ画面のHUD配置（現行仕様）

- HUDはプレイエリアへ**重ねる絶対配置**（`pointer-events-none` + `absolute inset-x-0 top-0 z-30`）で、レイアウト上の高さを持たない
- ただし**台形の頂点（画面上端・遠近projectionの基準点=yRatio 0）を覆う背景パネルは持たせない**。HUD自身の`<header>`要素には背景色・背景画像を一切付けず、文字には`text-shadow`だけで視認性を確保する
- **この「背景を持たせない」は JSX だけの話ではない。`monster-hero/index.html` のグローバルCSSから
  `[data-rhythm-hud]` へ背景・`backdrop-filter`・`box-shadow` を当てるのも同じく禁止**。
  実際に index.html 側で
  `background:linear-gradient(180deg,rgba(6,9,26,.98),rgba(7,9,24,.90))` +
  `backdrop-filter:blur(10px)` + `box-shadow:0 10px 28px …,0 1px 0 rgba(103,232,249,.12)`
  を当てており、画面上部に不透明な黒い帯ができて台形の頂点と背景を覆っていた。
  JSXしか読まない検査では見えず、実機で「中央上部に黒い帯が残っていてレーンが上まで見えない」と
  指摘されるまで検出できなかった（2026-09-03に撤去）。
  現在は `rhythm-hud-wedge-check.js` / `rhythm-landscape-hud-check.js` が index.html 側の
  `[data-rhythm-hud]` 向け規則も読み込んだうえで、計算済みスタイルの
  背景色・背景画像・影・ぼかしがすべて無いことを実ブラウザで確かめている
- プレイエリアの `box-shadow` も、上端側の `inset` 影は消失点付近を暗く塗り潰すので置かない（下端側のみ）
- HUD本文は、台形が奥へ向かって狭くなる分だけできる**左右の空きウェッジ**（`RHYTHM_PROJECTION_TOP_SCALE=.18`により、画面上端で全体の41%ずつが左右に空く）にだけ置く。中央の台形部分には何も置かない
  - 左ウェッジ（`data-rhythm-hud-left`）: 丸いランクバッジ＋次のランクまでの進捗バー(`rhythmRankProgress`) → スコア → BEST → 難易度バッジ＋TEST表記 → 曲名（2026-09-03、参考にした他アプリの見た目に合わせてランクを文字だけの表示から丸バッジ＋進捗バーへ変更）
  - 右ウェッジ（`data-rhythm-hud-right`）: ハートアイコン＋LIFEバー＋数値 → ポーズボタン（`pointer-events-auto`） → COMBO（同じくLIFEを見出し文字だけの表示からハートアイコン付きのバーへ変更）
- **ウェッジは下へ行くほど狭くなるので、行ごとに使える幅を変える**（1つの固定幅で全行を賄うと、いちばん下の行に合わせるしかなく上が無駄になる）。
  幅は画面幅基準の `vw` で指定する（`%` は親要素基準になり、入れ子だと意図した画面比にならない）。
  現行値: 左列 `max-w-[35vw]` / 難易度の行 `max-w-[34vw]` / 曲名 `max-w-[31vw]` / 右列 `max-w-[33vw]`
- **曲名は `truncate` で切らない。** 実機で「あつ杯テー…」と切れて曲が分からない状態になったため、
  `-webkit-line-clamp:3` で折り返して表示する。`rhythm-hud-wedge-check.js` が `truncate` の再導入を弾く
- 画面のいちばん上の中央（台形の頂点＝ノーツが湧く消失点）には何も置かない。ここを覆うのはPR #983で失敗した形
- 現在のプレイエリアは「画面の高さ − 下の余白8px」。iPhone 390×844相当で 701px → 836px（約19%増）
- **projectionの式は変更していない**。ただしプレイエリアが縦に伸びたぶん `travelPx` が増えるため、
  同じ `noteSpeed` でもノーツの見た目の速度は上がる。`travelMs`（`rhythmTravelMsForSpeed`）は変えていないので、
  判定タイミングと先読み時間は従来どおり
- 検査は `node tools/mode/rhythm-hud-wedge-check.js`（実ブラウザで、台形の頂点がHUDに覆われていないこと・HUD本文がそれが描かれる高さの台形へ一切かぶらないことを実測する）

#### Safe Area は body が1回だけ確保する（重要）

`monster-hero/index.html` の `body` が `height:100dvh; box-sizing:border-box; padding-top:env(safe-area-inset-top); padding-bottom:env(safe-area-inset-bottom)` を持っており、
`#root` 以下はすべて**すでにSafe Areaの内側**にある。したがってプレイ画面側で `env(safe-area-inset-*)` を足し直してはいけない。

以前は `main[data-rhythm-tap-test]` が `paddingTop:env(safe-area-inset-top)` を、HUDがさらに `calc(env(safe-area-inset-top) + 6px)` を持っていたため、
ダイナミックアイランド世代のiPhone（`env(safe-area-inset-top)=59px`）で**上に59px×2＝118pxの空白**ができ、
プレイエリアの高さも上下で計93px（上59＋下34）失われていた。実機で「上のスペースが広いまま」と指摘されて発覚した。

`rhythm-screen-layout-check.js` がこの二重掛けを、`rhythm-hud-wedge-check.js` が実測でのプレイエリア高さ（`画面 − 上Safe − 下Safe − 下余白8px`）を固定している。

#### 経緯（2026-09-02、実機プレイで発覚した失敗と、その原因）

最初にこの配置を試したとき（PR #983）、HUDに**全幅の背景パネル**（`background: linear-gradient(...)`を`inset-x-0`の`<header>`全体へ）を敷いてしまい、台形の頂点そのものを覆っていた。実機で確認したところ、

- 見えている台形の始まりが実際より進んだ位置になり、**遠近感が変わってプレイしづらくなる**
- パネルの不透明な高さぶん、プレイエリアのDOM上の高さが増えても**実際に見える範囲は増えない**（覆っているだけ）

という2つの問題が出たため、いったん元の横帯レイアウト（`header shrink-0`）へ戻した（PR #984）。

その後、**HUDに背景パネルを持たせず、台形の外側の左右ウェッジだけに文字を置く**（中央の台形には何も置かない）形に作り直したところ、台形の頂点は一切覆われず、遠近感を変えずにプレイエリアの高さを実際に拡張できることを確認した（PR #986）。

**この失敗が教えてくれたこと**: `measureTravel`の`spawnY`／`judgmentY`の基準（`playAreaRef`の実際のDOM矩形）を拡張すること自体は問題ではない。問題になるのは、その拡張した範囲を**不透明な要素で覆ってしまう**ことだった。今後この種の配置を変える場合は、「プレイエリアのDOM高さが増えたか」だけでなく「台形の頂点を含む可視範囲が本当に不透明物に覆われていないか」を必ず実機（または`rhythm-hud-wedge-check.js`のような、要素の背景色を実際にサンプルする検査）で確認すること。

### 入力フィードバック

- 5メインレーン表示を保ったまま、projectionに沿う入力発光を使う
- 指の実座標は10サブレーンへ逆投影してフィードバックする
- Touch接触幅が有効な場合は、現行の70%縮小・25%重なり条件で接触扱いになったサブレーンを発光対象へ反映する
- ノーツ・入力・レーン・判定位置の座標基準は別系統にしない

### 表示geometryの責務分担（現行仕様）

ノーツまわりの見た目は、次のように責務を分ける。ここを混ぜると、設定を変えた時だけレーンから
はみ出すといった不具合になる。

| 対象 | 基準 |
| --- | --- |
| ノーツの位置（親要素のleft / width / translate3d） | 共通projection |
| ノーツ頭の大きさ | ノーツサイズ設定（頭の`transform: scale`だけ） |
| HOLD帯・SLIDE帯 | レーンgeometry（プレイエリア幅の座標系＋projection） |
| ENDバー | レーンgeometry（終端レーンのprojection） |
| 入力hitbox・判定範囲 | ノーツサイズの影響を受けない |

ノーツサイズはプレイエリアのCSS変数 `--rhythm-note-size-scale` としてノーツ頭にだけ適用する。
親のノーツ要素へscaleを掛けると、子のHOLD帯・SLIDE帯・ENDバーまで一緒に拡大され、
帯が伸びた先の幅がclipPath / polygonの前提とズレる（速度が速いほど帯が長くなるため、
高速ほどズレが目立つ）。この形は採らない。

レーン枠・レーン境界線・サブレーン境界・サブレーン発光も、上端と下端の2点を直線で結ぶ台形にせず、
`rhythmProjectBoundary` を一定間隔（16分割）でサンプルした多点ポリゴンでノーツと同じ曲線へ沿わせる。
projectionは `pow(yRatio, 1.24)` の曲線なので、2点の直線近似では中間の高さで枠だけが
最大10px前後外側へ膨らむ。

**HOLD帯・SLIDE帯も同じ理由で多点にする。** 上端と下端だけを直線で結ぶと、

1. projectionが曲線であるぶん、途中の高さで帯がレーンから外れる
2. 帯が画面上端を越えて長いとき（＝高速時）、clipPathの0%は画面外のyを指すのに、幅は画面内へ
   クランプしたyで計算されるため、**可視範囲の全体が外側へ膨らむ**

という2つのズレが出る。2は速度が上がるほど帯が長くなるので、高速ほど大きくなる。実測では
速度10・HOLD幅1で中心が最大46.8px、幅が最大93.7pxずれていた。

対策として、HOLD帯は「帯の実際の上端（画面外でも可）から下端まで」を16分割し、SLIDEは
authored点の間を実時間で10分割して、それぞれ共通projectionへ沿わせる。HOLD帯は画面上端を
跨ぐときにその位置へも必ずサンプル点を置く（projectionの曲がりが一番きつい場所のため）。
この結果、ズレは中心0.49px・幅0.19px（SLIDEは中心0.61px・幅1.00px）まで下がる。

`node tools/mode/rhythm-note-geometry-audit.js` が、速度1／3／6／10／12 × サイズ80／100／120%の
全組み合わせでこの分担を実ブラウザ実測で検査する。

### ノーツ種別

| 種別 | 基本操作 |
| --- | --- |
| `TAP` | 判定ライン到達時にタップ |
| `HOLD` | 始点で押し、終端まで保持し、終端タイミングで離す |
| `FLICK` | 始点を押した後、一定距離以上を短時間でフリック |
| `SLIDE` | 始点で押し、指定経路を指で追従し、終端タイミングで離す |

1 authored note = 1最終判定を基本とし、HOLDやSLIDEの途中経過で判定数を増やさない。

ノーツを取得できない位置へ指を新しく置いたときは、`noteSeEnabled` / `noteSeVolume` に従う短い空押しSEを1回鳴らす。ノーツを現在保持していない指を押したまま横へ動かした場合は、10サブレーンの境界を越えて別サブレーンへ入るたび、その現在座標と既存projection・判定時間・幅1余白で開始判定を1回行う。境界1回につき取得は最大1ノーツで、成功時は既存の成功SEだけ、対象がない時は空押しSEを鳴らす。同じサブレーン内では再判定せず、往復方向でも境界ごとに判定する。TAP取得後は次の境界で別TAPを取得できるが、HOLD / SLIDE / FLICKなどを操作中の指は別ノーツを取得しない。判定文字とFAST / SLOWは同時に表示し、新しい判定のたびに表示期限を更新して約450ms後に同時に消す。

### WIDTH TEST

デバッグ専用曲「WIDTH TEST」で、以下を実入力確認できる。

- EASY: 幅1〜4 TAP、左右端・中央の幅1、隣接幅1同時押し、ワイドTAP、幅1→2→3→4、指腹接触の複数TAP確認
- NORMAL: 幅1〜4 HOLD / FLICK、左右端、隣接同時、HOLD中別TAP、FLICK+別TAP等
- HARD: 0.5レーン刻みSLIDE、折り返し、SLIDE中別TAP
- EXPERT: 幅1〜4 SLIDE、幅4中別TAP、幅1 SLIDE同時操作
- MASTER: S字、ジグザグ、0.5レーン往復、幅1→4→1、多数point、複雑SLIDE中の別TAP/HOLD

WIDTH TESTは正式譜面や一般公開導線へ追加しない。

---

## 3. 難易度と最大スコア（現行仕様）

| 難易度 | 最大スコア |
| --- | ---: |
| EASY | 600,000 |
| NORMAL | 700,000 |
| HARD | 800,000 |
| EXPERT | 900,000 |
| MASTER | 1,000,000 |

- 難易度ごとに別譜面を持つ
- 譜面内容やレベル表記は曲ごと・難易度ごとに定義する
- この最大スコア差は、将来の難易度混合スコアランキングでもそのまま意味を持つ

---

## 3.5. スコアランク（現行仕様・暫定値）

`RHYTHM_RANKS` / `rhythmRankForScore(score)` で、スコアから **G→F→E→D→C→B→A→S→SS→M**
の10段階ランク（Mが最上位）を算出する。プレイ中はSCOREの横に、リザルト画面には
大きなバッジで表示する。

**難易度ごとの割合(%)ではなく絶対スコアのしきい値で判定する。** %基準だと、EASYで100%を
出してもMASTERで100%を出しても同じ最上位ランクになってしまう。絶対値のしきい値にすることで、
EASYの最大スコア（60万点）はどれだけ極めてもAが上限になり、MASTERの100万点だけがMへ届く。

| ランク | しきい値 | 到達する難易度満点の目安 |
| --- | ---: | --- |
| M | 900,000〜 | EXPERT・MASTER満点 |
| SS | 800,000〜 | HARD満点 |
| S | 700,000〜 | NORMAL満点 |
| A | 600,000〜 | EASY満点 |
| B | 500,000〜 | — |
| C | 400,000〜 | — |
| D | 300,000〜 | — |
| E | 200,000〜 | — |
| F | 100,000〜 | — |
| G | 0〜 | — |

ランクはスコアからその場で計算するだけで、BESTの保存形式（`mh_rhythm_best_v1`）へは
何も追加していない。保存キーも増やしていない。

`node tools/mode/rhythm-rank-check.js` で確認する。

---

## 4. 判定・コンボ・達成（現行仕様）

| 判定 | 許容幅 | スコア率 |
| --- | ---: | ---: |
| MARVELOUS | ±25ms | 1.00 |
| EXCELLENT | ±50ms | 0.98 |
| GREAT | ±100ms | 0.90 |
| GOOD | ±150ms | 0.70 |
| BAD | ±200ms | 0.30 |
| MISS | ±200ms外 | 0 |

スコア構成:

- 判定: 90%
- コンボ: 10%

コンボ:

- MARVELOUS / EXCELLENT / GREAT / GOOD は継続
- BAD / MISS で切れる

達成:

- FULL COMBO
- ALL EXCELLENT
- ALL MARVELOUS

FAST / SLOW表示あり。

---

## 5. HOLD / SLIDE の終端判定とENDバー（現行仕様）

### 終端判定

- HOLD / SLIDE は終端タイミングで指を離す必要がある
- 終端の離し判定も開始時と同じ ±25 / 50 / 100 / 150 / 200ms を使う
- ±200msを超えた離しはMISS
- 終端から+200msを超えて押しっぱなしでもMISS
- 最終判定は「始点判定」と「終端判定」の悪い方
- SLIDEは途中の経路追従に失敗した場合もMISS
- HOLD / SLIDEを±200msの終端判定幅内で正常に離した場合は、開始入力と同じ仮ノーツSEを1回鳴らす
- 途中追従失敗、±200ms外、touchcancel / pointercancel、FLICKの指離しでは終端SEを鳴らさない

### 失敗したHOLD / SLIDEの見せ方（現行仕様）

MISSになったHOLD / SLIDEは、その場で消さず**譜面上の終端時刻まで薄いグレーで流し続ける**。
いきなり消えると「取り損ねたのか、そもそも無かったのか」が分からないため。

- 対象はHOLD / SLIDEのMISSのみ。TAP / FLICKと、終端を過ぎたノーツは従来どおりその場で消す
- 表示だけの扱いで、判定・コンボ・スコア・BESTには関与しない
- 一度失敗したノーツは復活しない（`done` のまま）
- 判定範囲外へ出ても終端までは表示を続ける

`node tools/mode/rhythm-failed-hold-trail-check.js` で確認する。

### 途中追従判定（現行仕様・暫定値）

HOLD / SLIDEは、始点・終端だけでなく途中の追従も継続的に確認する。1つの入力(pointermove /
touchmove、および毎フレームの再評価)ごとに、その時刻の正しい位置にいるかを見る。

- **SLIDE**: 経路(`slidePoints`)からの横ズレが `rhythmSlideTrackingTolerance`（既存、幅に応じて可変）を
  超えているかを見る
- **HOLD**: 帯の中心からの横ズレが「帯の半分幅 + 0.15レーン（0.3サブレーン、暫定値）」を
  超えているかを見る。従来はHOLDに横ズレ判定が無く、始点さえ取れば指をどれだけ動かしても
  通ってしまっていた
- 外れた瞬間には確定させず、**猶予（暫定120ms）を超えて外れたままの場合だけ**MISSを確定する。
  iPhoneの指ブレで一瞬だけ外れても即失敗にしないための猶予。猶予内に許容範囲へ戻れば
  カウントをリセットする（チェックポイントごとに判定を積み上げるのではなく、常に「今は
  外れているか」を見る）
- 途中で失敗を確定した場合、**指を離すのを待たずその場でMISSにする**。既存の
  「`endTimeMs` 到達で確定判定を呼ぶ」経路(scheduleTick)をそのまま使い、新しい判定経路や
  追加スコアは作らない。1 authored note = 1 最終判定は維持する
- 失敗が確定した直後から、上のグレー表示（薄い半透明）に切り替わる

猶予・HOLDの横ズレ許容は暫定値。実機確認のうえで調整する。
`node tools/mode/rhythm-mid-tracking-check.js` で確認する。

例:

- 始点 MARVELOUS + 終端 GOOD → 最終 GOOD
- 始点 GREAT + 終端 MARVELOUS → 最終 GREAT
- 始点 MARVELOUS + 終端 MISS → 最終 MISS

### ライフ（現行仕様・暫定値）

モンスターノーツ（マスモンの「回復 / バリア / 丈夫さ」）は、ライフの数値が決まっていないと
設計できない。そのためまず**数値の管理と表示だけ**を固定した。

| 項目 | 暫定値 |
| --- | ---: |
| 最大ライフ（`RHYTHM_LIFE_MAX`） | 1,000 |
| 開始時 | 満タン（1,000） |
| MARVELOUS / EXCELLENT | +2 |
| GREAT | +1 |
| GOOD | ±0 |
| BAD | -20 |
| MISS | -50 |

- 0未満・最大超過にはならないよう常にクランプする
- HUDのSCORE / COMBOの下に、残量に応じて色が変わるバーと数値を表示する
- ライフが0になったrunは**不可逆のDOWN**となる。以降は成功判定や将来の回復効果があってもライフを0に固定する
- 0へ到達した判定までは通常どおりスコアへ含め、その判定終了時点のスコアを`lockedScore`として固定する。DOWN後も判定・判定表示・FAST/SLOW・コンボ・判定数は継続するが、スコアは増えない
- ライフ0でも曲を強制終了せず最後までプレイできる
- リザルトscore、スコアランク、BEST更新判定は再計算せず同じ固定スコアを使う。保存形式と保存キー（`mh_rhythm_best_v1`）は変更しない

満タンからMISS 20回で0になる計算。EASYの78ノーツを基準に置いた暫定値であり、
**難易度ごとの調整はまだしていない**（MASTERのような高密度譜面では別の値が要る想定）。

未確定:

- DOWN状態のリザルト上の追加表示
- 難易度別のライフ最大値・減少量
- HOLD / SLIDEの途中失敗を、MISSと同じ減少量にするか

`node tools/mode/rhythm-life-check.js` で確認する。

### ENDバー

- HOLD / SLIDEの終端には、離すタイミングを示す専用ENDバーを表示する
- ENDバーが判定ラインへ到達したタイミングで指を離す
- 表示専用で、入力を遮らない
- runtimeが一時的に変更する `endTimeMs` ではなく、譜面本来の終端時刻を正本として位置を計算する
- HOLDはそのノーツの終端レーン、SLIDEは `slidePoints` の最終位置を優先して終端レーンを決める
- SLIDE終端が0.5レーン位置でも、その連続座標を保ったままENDバーを投影する
- 可変幅SLIDEのENDバーは最終 `slidePoints` の実効幅で投影する
- レーン・ノーツ・帯と同じprojection座標系で描画する

---

## 6. HOLD / 複数指（現行仕様）

- HOLD中に別レーンTAPを入力できる
- 同時2本HOLDに対応する
- 各指を独立して管理する
- ポーズ中は入力・自動MISS・曲時間を進めない
- 再開時は同一runの時間基準を維持する

---

## 7. FLICK / SLIDE（現行仕様）

### FLICK

- 方向指定なし
- 24px以上の明確な移動を検出
- 450ms以内
- `subLane`（0〜9）と `subLaneWidth`（1〜4）で開始位置と幅を指定できる
- 開始ノーツの描画と開始入力はTAP/HOLDと同じ10サブレーンprojectionを使う
- 幅1のみサブレーン0.18ぶんの入力余白を使う
- `subLane` 未指定の旧FLICKは従来のメインレーン幅2相当を維持する

### SLIDE

- `slidePoints` を時間補間して期待レーンを算出する
- authoredの始点・中継点・終点は0.5レーン刻みで指定できる
- authored点間の時間補間値は連続値のまま描画と追従判定へ使う
- `subLaneWidth` で1〜4のデフォルト幅を指定でき、未指定は幅2として旧仕様を維持する
- `slidePoints[].subLaneWidth` で各pointの幅1〜4を指定できる
- 実効幅は `point.subLaneWidth` → `note.subLaneWidth` → `2`
- point間は位置と同じ時間比率で連続補間する
- `lane` / `slidePoints[].lane` はSLIDE中心線であり、幅変更で中心線そのものは動かさない
- 始点の入力範囲と開始ノーツは開始時刻、操作中の頭と残り経路SVGは現在時刻、ENDバーは最終pointの実効幅へ合わせる
- 途中追従の中心は `rhythmSlideExpectedLane()` を使い、許容幅は現在時刻の補間幅に合わせる
- 幅2は従来の±0.82レーンを維持する
- 1本の指で経路を追従する
- 途中追従で追加スコア・追加判定を発生させない
- 終端では現在の終端リリース判定を使う

---

## 8. 再生・ポーズ・リスタート・リザルト（現行仕様）

- AudioContext基準の曲時間を正本にする
- ポーズ中は曲時間を止める
- 再開時は新しいBufferSourceを作り、保存したoffsetから再開する
- 完走時にリザルトを生成する
- 中断時はBESTを保存しない
- BESTは新スコアが過去BESTを上回った場合に更新する
- clear / fullCombo / allExcellent / allMarvelous 等の達成履歴は保持する
- リザルト再プレイやポーズリスタートは、旧runの状態を残さないようcleanupする

PR #901〜#904、#943等で画面遷移・再マウント・監視ループのフリーズ対策を行っている。自動検査成功とiPhone実機の見た目・体感確認は別物として扱う。

---

## 8.5 モンスターノーツ（第1弾実装済み・2026-09-03）

設定したマスモンをプレイ内容へ参加させる仕組み。詳細は
[`RHYTHM_FUTURE_IMPLEMENTATION_PLAN.md`](RHYTHM_FUTURE_IMPLEMENTATION_PLAN.md) §3〜§4。

譜面には **通常のTAPノーツへ `monsterSlot:1〜4` を1行足すだけ** で書く。

```js
{ type:'TAP', timeMs:8000, lane:2, subLane:4, subLaneWidth:2, monsterSlot:1 }
```

- `monsterSlot` はマスモン設定の何枠目か。1枠目→1個目、2枠目→2個目…と対応する
- 初期実装は **TAP専用**。HOLD / FLICK / SLIDE には付かない
- 判定・描画・幅の計算は通常ノーツの経路をそのまま通る。**判定窓は専用に甘くしない**
- 能力発動は **GREAT以上**。GOOD / BAD / MISS では不発
- 能力は **主血統** で決まる（元気 / 無敵 / 我慢 / 根性）
- 無敵（6秒）と我慢（15秒）は **別の能力としてそれぞれの残り時間で独立して走る**。
  両方有効なあいだは強いほう（無敵）が勝ち、無敵が切れたらそこから我慢の軽減へ変わる。
  逆に我慢が先に切れた場合は、無敵が残っているあいだダメージ0のまま
- ノーツ上のマスモンは枠・背景なしの染色済み透過画像だけを中央へ表示し、能力発動時はモンスター名を付けず「元気！」等の能力名だけを表示する
- DOWNから復帰できるのは根性だけ。DOWN中に根性をGREAT以上で取ると、その場でライフ50へ復活し、
  **その蘇生ノーツ自身は加算せず、次のノーツからスコア加算を再開する**

確認用に `monster_note_test`（MONSTER NOTE TEST・EASY・約40秒）を用意している。
既存の譜面は触っていない。

---

## 9. 保存キーとBEST（現行仕様）

保存キー:

- 設定: `mh_rhythm_settings_v1`
- BEST: `mh_rhythm_best_v1`
- モンスターノーツ用のマスモン設定: `mh_rhythm_monsters_v1`

これらを勝手に改名・削除・意味変更しない。

`mh_rhythm_monsters_v1` は **マスモンの個体IDの並び（文字列の配列・最大4件）だけ** を持つ。
名前・染色・能力はマスモン本体（`mh_masu_mons`）から毎回引き直す。音ゲー側へ複製すると
育成や染色の変更に追従できなくなるため、複製しない。

読み込み・保存時に行うのは **形の正規化だけ**（配列でなければ空、重複ID除去、4件で打ち切り）で、
「そのマスモンを今も持っているか」は見ない。マスモン一覧を読む前に存在確認まで行うと、
設定が空として保存し直されてプレイヤーの設定が消えるため。手放したマスモンと
同一ベースモンスターの重複は、**使うときだけ**（`resolveRhythmMonsterSlots`）落とす。

曲×難易度ごとにBESTを保存する。

保持対象:

- `bestScore`
- `maxCombo`
- `clear`
- `fullCombo`
- `allExcellent`
- `allMarvelous`
- 各判定数

欠損値・旧データ・不正値は読み込み時に正規化する。

現行ノーツSE設定は `noteSeEnabled` / `noteSeVolume` を同じ設定キー内で扱う。

---

## 10. 公開状態（現行仕様）

現時点では音ゲーモードは開発中。

- 一般公開しない
- 通常HOME等へ公開導線を追加しない
- デバッグ画面から確認する
- 週間ランキング等もまだ一般公開しない
- 一般公開へ切り替える場合はユーザーの明示指示を必要とする

---

## 11. 現行デバッグ・譜面制作基盤

### 座標校正

PR #942以降、デバッグ用の座標校正ガイドを持つ。

- 5メインレーン境界
- 10サブレーン境界
- 深度20 / 40 / 60 / 80%
- 判定ライン
- TAP / HOLD / FLICK 幅1〜4の基準
- SLIDE 幅1〜4・0.5レーン基準
- 判定ライン上の10サブレーン中心

通常判定ロジック自体は変更しない。

座標校正のトグルは、以前は `document.body` 直下へ `position:fixed` で置き、プレイエリアがある間は
常に表示していた。これがプレイ画面のHUDへ重なるため、現在は**その画面自身のUIの中**へ置く。

- 音ゲーデバッグ画面（プレイ前に切り替える）
- ポーズメニュー（プレイ中に切り替える）

ON/OFFの状態は画面をまたいで保持する。音ゲーの各画面は「固定ヘッダー＋スクロール1つ（＋固定フッター）」
だけで閉じ、画面をまたぐ固定レイヤーを増やさない。`node tools/mode/rhythm-screen-layout-check.js` で確認する。

### 音ゲーデバッグ画面のタブ構成（現行仕様）

デバッグ画面は、設定ダンプ・曲一覧・譜面エディタ・実機タイミング補正・視覚エディタ・レビューUIを
すべて1本のスクロールへ積んでいたため、入った瞬間に何がどこにあるか分からない状態になっていた。
現在は固定ヘッダーの下に3つのタブを置き、本文スクロールは1つだけにする。

| タブ | 内容 |
| --- | --- |
| ▶ プレイ | 曲・難易度の一覧とテストプレイ（**入場時は必ずここから始まる**） |
| 🎼 譜面制作 | 譜面エディタ一式（実機タイミング補正・視覚エディタ・レビューUIもこの中） |
| ⚙️ 設定・記録 | 座標校正トグルと、保存中の設定の一覧 |

譜面制作UIは**一度もタブを開いていない間は生成しない**（初回入場を軽くし、ごちゃつきを避けるため）。
一度開いた後はDOMを残して表示だけ切り替える。エディタの編集中ドラフトはmount時のクロージャが持つので、
DOMごと外すとドラフトが失われるため。

### 判定ライン・描画負荷

PR #946で、判定位置を分かりやすくするためSVG判定ラインを固定3層線＋5レーン目印へ強化した。

同時にiPhone Safariの描画負荷対策として、判定ラインのGaussianBlurを廃止し、プレイ中の不要な全画面MutationObserver再検索等を抑制した。

### 譜面エディタ

PR #944以降、デバッグ専用譜面エディタを持つ。PR #948〜#952でスマホ向け視覚編集を段階的に追加した。

現行で扱えるもの:

- TAP / HOLD / FLICK / SLIDE
- 10サブレーンの縦タイムライン表示
- タイムライン空白タップでの直接配置
- ノーツ選択と拍・レーンのPointerドラッグ移動
- 幅1〜4のワンタップ変更
- HOLD終端の専用ハンドルによる直接伸縮
- SLIDE始点・終点・中継点の視覚表示と直接ドラッグ
- SLIDE authored位置を0.5レーン刻みへスナップ
- SLIDE終点・中継点時刻を16分グリッドへスナップ
- SLIDE経路タップで中継点追加、選択中継点の削除
- SLIDE始点・終点・`slidePoints` を既存draft形式のまま保持
- 固定拍グリッドへのスナップ
- 音源再生・シーク
- 再生位置ライン・再生追従
- 現在の再生位置から開始/終了を指定する16分グリッド区間ループ
- 区間ループ範囲のタイムライン帯表示、開始シーク、解除
- TAP / HOLD / FLICK のサブレーン範囲・幅はみ出しチェック
- HOLD / SLIDE の終端時刻・1〜128グリッド長チェック
- SLIDE の0.5レーン、2点以上、時刻順、始終点と `slidePoints` の整合チェック
- 固定16分グリッド外配置のエラー表示
- 同時刻・同位置の重複ノーツは警告表示（禁止はしない）
- 不正ノーツの一覧・視覚タイムライン強調表示
- 不正配置がある状態でのテストプレイ・JSON / 実装JS書き出し停止
- JSON / 実装JS出力
- 編集中譜面の実プレイ確認

視覚編集レイヤーはデバッグ画面でのみ遅延読込し、通常プレイ・正式譜面・セーブ/BESTへ直接書き込まない。複雑SLIDEは専用経路編集を使い、通常のノーツ本体ドラッグで中継点を破壊しない。区間ループも編集用audioのみに作用し、実プレイruntimeへは適用しない。不正配置チェックも制作draftを保護するデバッグ専用機能であり、正式runtimeの判定仕様や保存形式は変更しない。

正式なユーザー向け機能ではない。

### EASY自動ドラフトと正式候補v1

PR #945で「あつ杯テーマ」EASYの自動ドラフト100ノーツを既存の実プレイruntimeへ接続した。PR #953で実音源オンセットとの音ハメ基礎監査を追加し、PR #954で正式譜面制作へ進むための「正式候補v1」をデバッグエディタへ接続した。

自動ドラフト100ノーツ:

- TAPのみ
- 5メインレーンへ仮分散
- タイミング・密度・候補抽出の比較元として残す
- **正式譜面ではない**

正式候補v1:

- 78ノーツ
- TAP 72 / HOLD 6
- 幅2、5メインレーン中央のみを使用
- 元100候補のうち、実音源ピークと16分グリッドの差が±30ms以内の78点を採用
- ±30msを超えた22点は削除確定ではなく `earReviewGrids` として保留
- HOLDは4または8グリッド長の制作候補で、終端は次ノーツより前に収める
- デバッグ譜面エディタ起動時は正式候補v1を優先読込し、旧100ノーツは比較用/fallbackとして残す
- 既存の音ゲー入力・判定runtimeを使ってデバッグ実プレイできる
- `reviewRequired:true` / `runtimeConnected:false` のままで、正式runtime・BEST・一般公開には接続しない
- **耳確認・フレーズ調整前の制作候補であり、正式完成譜面ではない**

---

## 12. テスト曲「あつ杯テーマ」とタイミング基準

曲:

- 表示名: あつ杯テーマ
- 内部ID: `atsu_cup_theme`

現在の譜面制作基準:

- BPM: 169
- 1拍: `60000 / 169` ≒ 355.03ms
- `beatZeroMs`: 40ms
- 基本分割: 1拍4分割（16分グリッド）

実音源解析で約169 BPMまで絞り込んだ値を制作基準としている。

PR #953の100ノーツ音ハメ基礎監査:

- 平均ピーク差: -5.8ms
- 中央ピーク差: -11ms
- 平均絶対差: 20.3ms
- 中央絶対差: 20ms
- ±30ms以内: 78%
- ±40ms以内: 95%
- 時間ドリフト傾向: -3.4ms/分
- 前半1/4→後半1/4の平均差: -7.7ms
- ±30ms超の要耳確認: 22点

この監査では曲後半ほど大きくずれ続ける傾向は見られず、169 BPM / beatZero 40msの基礎グリッドは正式譜面制作を続ける土台として採用する。ただし、これは実音源のオンセットと固定グリッドの数値整合であり、メロディ・フレーズ・キメに対する演奏感まで自動で保証するものではない。

### あつ杯テーマ DEBUG 60s

- `songId`: `atsu_cup_theme_debug_short`
- 既存の `atsu_cup_theme`（`audio/bgm-atsu-cup-theme.mp3`）を0秒から再利用し、音源の複製・編集は行わない
- 約60秒で既存audio clockと通常リザルトを使って終了するDEBUG専用曲
- TAP / HOLD / FLICK / SLIDE、幅1〜4、複数指、HOLD／SLIDE中の別TAP、0.5レーン・可変幅SLIDEを1回で確認する総合回帰テスト用
- 正式EASY候補とは別データであり、正式譜面制作・フル尺候補へ接続しない
- 純粋な入力・幅・SLIDE構造検査用のWIDTH TESTとも役割を分ける
- 通常HOME、正式曲選択、一般ユーザー導線には公開しない

### EASY正式候補v2レビュー準備

実装済み:

- v1の正式候補78ノーツ（TAP 72 / HOLD 6）と22耳確認候補を変更せず保持
- v1採用78点の全尺offset・区間偏り・drift・32grid密度・隣接間隔・長い空白・HOLD後余白を機械監査
- `atsu-cup-theme-easy-formal-candidate-v2-review.json` に78ノーツと22件の `pendingReviews` を分離して保存
- 22候補ごとに時刻、offset、strength、前後採用点、近傍±2grid onset、機械推奨を記録。機械推奨は参考情報であり正式採否ではない
- iPhone向け16区間ループ、候補単位ナビ、採用／前後1grid移動／削除／保留、レビューJSONコピー、DEBUGプレビュー仮適用
- 約60秒のDEBUG総合回帰曲は引き続き基盤回帰用として維持

未完了:

- ユーザーによる22候補の耳確認
- 22候補の正式な採用・移動・削除
- EASY正式譜面の完成
- 正式runtimeへの接続

v2-reviewの `status` は `FORMAL_CANDIDATE_REVIEW`、`reviewRequired=true`、`runtimeConnected=false` とし、未確認候補を正式notesへ自動追加しない。

重要:

- 解析値だけで本番譜面を完成扱いにしない
- 曲を聴きながら拍・小節・フレーズ単位で確認する
- ±30ms超の22点は区間ループとiPhone実機で耳確認してから採用・移動・不採用を決める
- 曲中ずっと一定方向へズレる場合はBPM、曲全体でほぼ同じ量だけズレる場合は全体offsetを疑う
- PR #946のデバッグプレビューでは `-20 / -10 / 0 / +10 / +20ms` の実機確認用補正を持つ
- この補正は正式譜面やBESTへ保存しない
- 最新のiPhone実機確認ではユーザーから「問題なさそう」と確認済み。現時点で基礎タイミング・レーン・入力・判定位置は一旦固定して次工程へ進む

---

## 12.5 体験版の先行公開曲「Monster Hero」（EASY / NORMAL / HARD 正式候補v1）

体験版で先行公開する曲は **Monster Hero** で、既存BGM `monster_hero_theme`
(`monster-hero/audio/bgm-monster-hero-theme.mp3`) をそのまま使う。体験版のために音源を複製しない。

### タイミング基準（`data/rhythm-timing.js` の `monster_hero_theme`）

| 項目 | 値 |
| --- | --- |
| 音源の長さ | 152,761ms（2分32秒） |
| BPM | 173.153 |
| 1拍 | 346.515ms |
| `beatZeroMs` | 206ms |
| 基本分割 | 1拍4分割（16分グリッド＝86.63ms） |

実音源を1msエンベロープへ落とし、16分グリッドとの残差が最小になるBPM・位相を全尺で探索して決めた。

- 検出ピーク数: 1,384
- 平均絶対差: 17.6ms / 中央絶対差: 15.9ms
- ±30ms以内: 78.8% / ±40ms以内: 95.7%
- 前半→後半の平均差の変化（ドリフト）: -1.2ms

前半・後半それぞれで求めたBPMの差は0.04未満で、曲中ずっと一方向へずれ続ける傾向は見られない。
このグリッドを譜面制作の土台として採用し、全体オフセットは実機の耳確認後に必要なら微調整する。

### 譜面候補の作り方（3難易度共通）

`tools/mode/rhythm-monster-hero-chart-build.js` が、実音源のオンセット候補から**決定的に**組み立てる。
乱数を使わないので、同じ入力からは必ず同じ譜面になる。方針を変えるときは譜面を手で書き換えず、
ツールの `PROFILES` を直して作り直す。

候補ファイルは2つある。どちらも同じ音源・同じグリッドで、拾う音の強さのしきい値だけが違う。

| ファイル | しきい値 | 候補数 | 使う難易度 |
| --- | --- | --- | --- |
| `monster-hero-theme-onset-candidates.json` | 0.60 | 372 | EASY |
| `monster-hero-theme-onset-candidates-dense.json` | 0.30 | 604 | NORMAL / HARD |

EASYはすでにレビューへ出しているため、候補ファイルも強さの下限も動かさない。
NORMAL / HARD は刻みやゴーストノートまで拾ったdense側から、それぞれの強さで絞る。

共通の決まり:

- 採用するのは実音源ピークとの差が**±30ms以内**のものだけ（あつ杯テーマ正式候補v1と同じ基準）
- ±30msを超え42ms以内は削除確定ではなく `earReviewGrids` として保留
- 小節（16グリッド）ごとに、その小節の強さに応じた本数だけ選ぶ
- HOLDは曲全体へ等間隔で散らす（先頭から詰めると前半へ固まるため）
- モンスターノーツは4体ぶんを 20 / 40 / 60 / 80% 付近へ、前後を1拍以上空けて置く
- 曲の頭1.8秒と終わり1.2秒は空ける

### 難易度ごとの制作方針

| | EASY | NORMAL | HARD |
| --- | --- | --- | --- |
| ノーツ数 | 176 | 212 | 266 |
| 密度 | 1.22/秒 | 1.45/秒 | 1.80/秒 |
| 種別 | TAP / HOLD | ＋FLICK | ＋SLIDE |
| 内訳 | TAP166 HOLD10 | TAP186 HOLD14 FLICK12 | TAP228 HOLD14 FLICK16 SLIDE8 |
| 置き場所 | 8分のみ | 8分のみ | 16分裏も使う |
| 幅 | 2固定 | 1 / 2 / 3 | 1 / 2 / 3 / 4 |
| レーン移動 | 1つまで | 2つまで | 3つまで |
| 同時押し | なし | なし | HOLD/SLIDE中の別TAPのみ |
| 耳確認へ保留 | 25件 | 37件 | 78件 |

補足:

- **EASY** … この曲のオンセットは8分に集中しているため、置き場所も8分に限る。16分裏へは置かない。
  同時押しを作らないので、隣接する幅1TAPを並べないという可変幅1ノーツ化の制作ルールにも自動的に従う。
  8分でつながる2個は同じレーンへ置く（左右へ振るより易しいため）
- **NORMAL** … EASYと同じ8分の土台のまま、密度と左右の動きを増やす。
  可変幅を自然に使い、強い音ほど広く、速い連打は狭くする。FLICKはフレーズの切れ目へ12件
- **HARD** … 16分裏も使う（ただし音がある位置だけ）。SLIDEは拍頭を起点に12グリッドの区間ごと
  置き換える形で8件置き、0.5レーン刻み・途中で幅が変わる経路にする。
  HOLD/SLIDEの最中に、長押ししている指から4サブレーン以上離してTAPを重ねる複合操作を12件入れる

### 公開状態

- `songId`: `monster_hero_theme_candidate` / 表示名「Monster Hero 候補」
- デバッグ画面の曲一覧からだけ選べる。正式な曲選択・BEST・一般ユーザー導線へは出さない
- 3難易度とも `reviewRequired:true` / `runtimeConnected:false`
- 短縮再生（`playDurationMs`）は持たない。全尺で遊ぶ
- EXPERT / MASTER は空のまま（体験版の対象外）

### 未完了

- ユーザーによるiPhone実機での耳確認（拍・小節・フレーズ・キメに合っているか）
- `earReviewGrids`（EASY 25 / NORMAL 37 / HARD 78点）の採用・移動・不採用の判断
- 密度・レーンの流れ・HOLD/FLICK/SLIDEの位置の手直し
- 3難易度の難易度差が自然かの確認
- 正式runtime・BESTへの接続

**この検査（`tools/mode/rhythm-monster-hero-chart-check.js` / 109項目）が全部通っても
正式完成譜面にはならない。** 機械的に確かめられるのは配置の規則だけで、
曲に合っているかは実機の耳確認でしか決められない。

### 体験版の正式導線（実装済み・2026-09-03）

デバッグ画面をそのまま公開しないために作った、正式導線の最小構成。

| 画面 | `gameState` | 内容 |
| --- | --- | --- |
| 体験版ホーム | `RHYTHM_DEMO_HOME` | 「体験版」バッジ、調整中の断り、曲、難易度3つ、自己ベスト、設定・マスモン設定への入口 |
| マスモン設定 | `RHYTHM_DEMO_MONSTERS` | デバッグ画面と同じ `RhythmMonsterSlotsPanel` を使う |

- 体験版で遊べる範囲は `RHYTHM_DEMO_SONG_ID` / `RHYTHM_DEMO_DIFFICULTY_IDS` としてデータ側で決める。
  デバッグ用の曲一覧（`RHYTHM_SONGS`）とは役割を分け、デバッグ曲を体験版へ出さない
- 譜面が空の難易度はボタンに出さない（押せるのに始まらない状態を作らないため）
- 自己ベストはスコア・ランク・最大コンボを出し、未プレイなら「まだ遊んでいません」と出す
- 体験版から始めたプレイは体験版ホームへ戻る（`rhythmPlay.from==='demo'`）。
  設定画面の戻り先も `rhythmOptionsBack` で開いた画面へ返す
- 保存キーは増やしていない。設定・BEST・マスモンは既存の `mh_rhythm_*` をそのまま使う
- 入口は当面デバッグ画面のみ。`RHYTHM_MODE_PUBLIC_RELEASE` が true になったら戻るボタンがHOMEを指す

### HOMEの入口（2026-09-03にユーザーが決定）

**修行の施設をやめ、その場所を音ゲーに譲った。**表示名は略称の **「モンビー」**
（正式名称はモンヒロビート）。HOMEの施設ボタンは横幅が狭く、正式名称そのままでは収まらないため。

- CSSは `.mh-home-facility.training` を `.mh-home-facility.rhythm` へ置き換え、位置
  （`left:0; top:46%; width:38%; height:25%`）はそのまま引き継いだ
- `RHYTHM_MODE_PUBLIC_RELEASE` が **false** のあいだは、修行と同じように
  タップしても「準備中」の案内（`RHYTHM_INFO`）だけを出し、本編からは遊べない
- **true** にすると、そのまま体験版ホーム（`RHYTHM_DEMO_HOME`）へ入る。切り替えは この1行だけ
- 準備中の案内は修行のCSS（`mh-training-*`）を借りず、体験版ホームに合わせた自前の見た目にした
- 修行の実装（`TRAINING_*`）は消していない。HOMEから外しただけで、デバッグ画面の
  「修行テスト」からは今までどおり確認できる
- ヘルプには公開フラグで伏せない項目 `basics/rhythm-coming-soon` を置き、
  準備中の案内画面もヘルプから説明できるようにした

検査は `tools/mode/rhythm-demo-entry-check.js`（33項目）。

---

## 13. 実装履歴の主要段階

| 段階 | 内容 |
| --- | --- |
| STEP 1 / PR #862 | 5レーン・4ノーツ・5難易度・判定・保存・デバッグ基盤 |
| STEP 2B / PR #867 | TAP実プレイ、ポーズ、再開、リスタート、リザルト、BEST |
| STEP 3A / PR #868 | HOLD、複数指入力 |
| STEP 3B / PR #884 | FLICK、SLIDE |
| PR #890/#891/#900 | レーン・ノーツ・入力projectionの統一、見えるレーンのSVG化 |
| PR #901〜#904 | リザルト再プレイ・ポーズ操作の再マウント/タッチ経路修正 |
| PR #905 | SLIDE帯の残り時間表示 |
| PR #906 | HOLD / SLIDE終端リリース判定 |
| PR #908 | 遠近感を強化し、レーン・ノーツ・帯・入力の投影位置を再統一 |
| PR #910 | HOLD / SLIDE終端ENDバー |
| STEP 1A | 10サブレーン・TAP幅1〜4の描画 |
| STEP 1B | 可変幅TAP入力・幅1タッチ許容・WIDTH TEST |
| PR #919 | TAP / HOLD / FLICK / SLIDEの正常な開始入力へ仮ノーツSE |
| PR #920 | HOLD / SLIDEの正常な終端リリースへ仮SE |
| PR #921 | SLIDEの始点・中継点・終点を0.5レーン刻みへ対応 |
| STEP 2B-2〜4 | SLIDE可変幅・point幅補間・複雑SLIDEテスト |
| STEP 2C | FLICKへ10サブレーン配置と幅1〜4 |
| PR #934 | 押しっぱなし横移動、10サブレーン発光、空押し境界SE、判定表示自動消去 |
| PR #935 | iPhone Touch `radiusX` 接触幅入力の初期実装 |
| PR #937 | 押しっぱなし時の自動TAP抑制・発光レイヤー調整 |
| PR #939 | `radiusX` の固定最大3制限を撤去し、異常値guardへ変更 |
| PR #940 | 指腹接触を70%へ縮小、隣接25%以上重なり条件を採用 |
| PR #941 | 押しっぱなし中の中心deadzone・明確な指腹拡張追従 |
| PR #942 | 座標校正デバッグガイド |
| PR #943 | MutationObserver自己発火によるフリーズ修正 |
| PR #944 | 譜面エディタv1 |
| PR #945 | EASY自動ドラフト100ノーツを実プレイへ接続 |
| PR #946 | 判定ライン明確化、実機±ms補正、描画・監視負荷軽量化 |
| PR #947 | 音ゲー現行仕様・将来計画を本設計書へ統合 |
| PR #948 | 視覚エディタSTEP 1: 10サブレーンタイムライン・直接配置・選択/移動・幅変更・再生追従 |
| PR #949 | 視覚エディタSTEP 2-A: HOLD終端の直接伸縮 |
| PR #950 | 視覚エディタSTEP 2-B: SLIDE始点・終点・中継点の視覚経路編集 |
| PR #951 | 視覚エディタSTEP 2-C: 編集用audioの16分グリッド区間ループ |
| PR #952 | 視覚エディタSTEP 2-D: 不正配置チェック・重複警告・不正時のプレビュー/書き出し停止 |
| PR #953 | あつ杯テーマEASYの音ハメ基礎監査をCIへ追加 |
| PR #954 | あつ杯テーマEASY正式候補v1: 78ノーツ（TAP72/HOLD6）、22点を耳確認保留、デバッグ実プレイ接続 |
| PR #967 | 音ゲーオプション STEP1 |
| PR #970 | 保存済みオプションをruntimeへ反映 |
| PR #971 | 画面構造の分離（座標校正トグルをfixedから各画面内へ）、ノーツ速度1.0〜12.0/0.1刻み、ノーツサイズをノーツ頭だけへ適用、レーン枠の多点projection化、速度×サイズの実測回帰 |

---

# ここから先は将来仕様・残作業

## 14. 正式譜面制作方針（確定・一部実装）

基礎操作・レーン・判定位置・実機安定性が確認できたため、今後は安易にタッチ感覚を再調整せず譜面制作へ進む。

制作順:

1. スマホで扱いやすい視覚譜面エディタを完成させる
2. 「あつ杯テーマ」EASYを正式なフル尺譜面として完成させる
3. NORMAL
4. HARD
5. EXPERT
6. MASTER
7. 複数曲へ本番譜面を展開する

正式譜面は自動解析・自動ドラフトだけで完成扱いにせず、人が曲を聴いて確認する。

### 視覚譜面エディタの目標

PR #948〜#952時点の進捗:

- 10サブレーン上へ直接配置: **実装済み**
- TAP / HOLD / FLICK / SLIDEを視覚的に配置・編集: **実装済み**
- 幅1〜4を簡単に変更: **実装済み**
- HOLD長さを編集: **実装済み**
- SLIDE経路を視覚編集: **実装済み**
- 拍スナップ: **固定16分グリッドは実装済み**
- 再生 / 停止 / シーク: **実装済み**
- 区間ループ: **実装済み**
- 編集譜面の即テストプレイ: **実装済み**
- 不正配置チェック: **実装済み**

確定していた視覚譜面エディタの主要目標は一通り実装済み。次段階は「あつ杯テーマ」EASYの正式フル尺譜面制作へ進む。Undo/Redoは便利機能候補だが、現時点では確定仕様として扱わない。

拍スナップは現状16分固定。1/4、1/8、1/12、1/16等の複数スナップ切替は未確定であり、必要性を正式譜面制作時に確認してから決める。

### あつ杯テーマ EASY の現在地

PR #954の正式候補v1まで実装済み。ただし、これは完成譜面ではない。

次に行うこと:

1. `earReviewGrids` の22点を区間ループ＋iPhone実機で耳確認する
2. 曲のフレーズ、キメ、伸ばし音に合わせてTAP/HOLDの位置・種類・密度を調整する
3. 必要なら22点から採用、近傍グリッドへ移動、または不採用を決める
4. 全尺を実プレイし、EASYとして無理のない密度・左右移動・HOLD操作か確認する
5. 問題がなくなった段階で正式EASY譜面としてruntimeへ接続する

耳確認が終わるまで `reviewRequired:true` を維持し、「正式完成」「本番接続済み」とは扱わない。

---

## 15. 正式音ゲーホーム（確定・未実装）

音ゲー正式公開時には、デバッグ画面とは別に音ゲー専用HOMEを持つ。

HOMEに必要な機能の方向性:

- 楽曲選択
- 難易度選択
- 自己ハイスコア表示
- クリア・達成状況確認
- ランキングへの導線
- オプションへの導線
- マスモン設定・表示
- プレイ開始

具体的な画面配置・カード構成・表示順は未確定。スマートフォン縦画面と片手操作を優先する。

---

## 16. オプション STEP1（デバッグ導線へ実装済み）

正式公開時にも再利用する音ゲー専用オプションを、現在は音ゲーデバッグ画面の「⚙️ オプション」から開ける。通常HOMEや一般公開導線には接続しない。保存先は既存の `mh_rhythm_settings_v1` を後方互換で拡張し、旧データが `noteSeEnabled` / `noteSeVolume` だけでも欠損項目を現行相当の既定値へ正規化する。

画面は「音量」「プレイ」「表示」「演出・端末」の4カードで、次を設定できる。

- BGM音量 0～100（音ゲー再生だけへ適用）と直接タップによるBGM試聴
- タップ音量 0～100、タップ音 ON/OFF、直接タップによるSE試聴
- ノーツ速度 1.0～12.0（0.1刻み、6.0が従来流速）
- ノーツサイズ 80～120%（5%刻み、入力hitboxは不変）
- 判定タイミング調整 -100～+100ms（5ms刻み）
- FAST/SLOW表示、判定文字表示
- レーン発光（標準／控えめ／なし）
- 演出量（標準／少なめ／最小）、振動、軽量モード

「デフォルトに戻す」は編集中の値だけを戻し、「保存」で初めて確定する。操作ボタンはiPhoneのSafe Areaを避けた画面下固定バーに置き、設定項目だけをスクロールする。未保存変更がある場合は保存ボタンを強調する。振動の既定値はOFFで、Vibration APIがない端末でも失敗しない。

BGM音量・タップ音量・ノーツ速度・ノーツサイズ・判定タイミング調整は、横長スライダーではなく、現在量の簡易ゲージを挟んだ「−／＋」ボタンで1段階ずつ変更する。現在値は常時表示し、最小値・最大値では対応するボタンを無効化する。範囲・刻み・保存形式・runtime計算は上記の現行仕様を維持する。

ノーツ速度は見た目の移動時間（travel）だけを変更する。整数速度を基準点として0.1刻みで線形補間し、6.0は従来相当の2150msを厳密に維持する。基準点は 1.0=7000 / 2.0=6000 / 3.0=5000 / 4.0=4000 / 5.0=3000 / 6.0=2150 / 7.0=1680 / 8.0=1300 / 9.0=1020 / 10.0=800 / 11.0=630 / 12.0=500（ms）で、低速側は等差、高速側は約1.27倍ずつの等比になっている。どの帯域でも0.1動かせば必ずtravelが変わる。authored noteTime・BPM・beatZero・判定窓・入力時刻・スコア・曲時間・HOLD/SLIDEのauthored timingは変更しない。旧保存値（3.0〜10.0 / 0.5刻み）は新しい範囲の内側なのでそのまま読み込める。

ノーツサイズは**ノーツ頭の描画scaleだけ**を変更する。HOLD帯・SLIDE帯・ENDバー・入力hitboxはノーツサイズの影響を受けず、常にレーンgeometry基準のままにする。判定タイミング調整は同一の `judgmentTimingOffsetMs` を表示時計・入力・HOLD終端へ適用し、判定窓幅、BPM、noteTime、スコア計算は変更しない。FAST/SLOW・判定文字・レーン発光は表示だけを変える。演出量は彩度と非必須グロー/影を段階的に抑え、軽量モードは非必須グロー/影とtransitionを停止するが、入力判定、DOM判定ライン、ノーツ、レーンは残す。

正式HOMEへの入口と、正式公開時の最終デザインは未実装。

### 音量の独立性（現行仕様）

音ゲーのBGM／タップ音量は、メインゲーム（HOME）の音量設定とは**完全に独立**している。

- 音ゲーBGM: `Audio_.startRhythmTrack` が作る専用の `rhythmGain` ノードを、メインのBGM音量が
  かかる `bgmGain` を経由せず `ctx.destination` へ直結する。曲ごとの音量差は `safeTrackGain` で
  正規化する。**以前はrhythmGainがbgmGainへ直列に繋がり、両方の音量設定が掛け算になっていた**
  （メイン側のBGM音量を下げると音ゲー側をどれだけ上げても小さいまま・無音になる不具合があった）
- タップ音（SE）: もともとTone.js／`seBus`を使わず専用のAudioContextで鳴らしており、メインの
  SE音量とは無関係
- **全体ミュート（タイトル画面の「音がオフです」）だけは共通**に効く。`Audio_.setEnabled` が
  `window.__mhAudioEnabled` へ反映し、稼働中の音ゲーBGMのgainと、タップ音の`rhythmAudioGloballyEnabled()`
  判定の両方がこれを見る

`node tools/mode/rhythm-audio-independence-check.js` で確認する。

---

## 17. マスモン連携（確定・未実装）

音ゲーモードでもMonster Hero本編との互換性を持たせる。

確定している方向性:

- 所有モンスターから音ゲー用の「マスモン」を設定できるようにする（**設定と保存はPR #995で実装済み**）
- マスモンを音ゲー画面内のどこかへ表示する（**モンスターノーツとしての表示はPR #997で実装済み**）
- 曲・BPMに合わせてマスモンが動く演出を持たせる
- 音ゲープレイでマスモンへ経験値が入る仕組みを持たせる

目的:

`音ゲープレイ → スコア/達成 → ランキング/報酬 → マスモン育成`

というMonster Hero本編へつながるループを作る。

未確定:

- マスモン表示位置
- 具体的なビートアニメーション
- コンボ・フルコン等へのリアクション
- 経験値量
- 難易度・スコア・コンボ等による経験値補正
- 本編の既存育成効率との相対バランス

経験値量は本編育成効率を壊さないよう、実装前に現行mainの経験値取得量と比較して決める。

---

## 18. ランキング全体方針（確定・未実装）

正式音ゲーには以下を用意する。

1. 自己ハイスコア
2. 永続全国ランキング
3. 週間ランキング

### 自己ハイスコア

- 曲×難易度ごとのBESTは現行 `mh_rhythm_best_v1` に存在する
- 正式HOME / 曲選択 / リザルト等から分かりやすく表示する

### 永続全国ランキング

- 永続する全国スコアランキングを用意する
- 週間リセットとは別物
- 具体的な集計単位、難易度を統合するか、同点処理、保存構造、Supabase構成はまだ最終確定していない
- 現時点で勝手にSupabase実装を先行しない

### モンスターノーツ能力の扱い（方針確定・2026-09-03）

マスモン能力込みのプレイは、**そのまま正式な攻略要素としてランキングへ許可する**。
使用有無で部門を分けたり、除外ランキングを作ったりしない。マスモン設定を途中で
変えても、BEST / ランキング記録には何も残さない（現行の `mh_rhythm_best_v1` の
構造を増やさない）。詳細は
[`RHYTHM_FUTURE_IMPLEMENTATION_PLAN.md`](RHYTHM_FUTURE_IMPLEMENTATION_PLAN.md) §5。

---

## 19. 週間対象曲と4部門ランキング（確定・未実装）

週間システムは、**正式譜面が完成している曲が3曲以上揃ってから実装する**。

### 対象曲

毎週、本番譜面が完成している曲の中から3曲を週間対象曲として選ぶ。

- 曲A
- 曲B
- 曲C

曲として登録されているだけでは対象にしない。ランキングへ使用できる正式譜面が完成していることを条件とする。

### ランキング部門

週間ランキングは合計4部門。

1. 曲A スコアランキング
2. 曲B スコアランキング
3. 曲C スコアランキング
4. 週間総合スコアランキング

### 難易度の扱い

**各曲ランキングを難易度別ランキングへ分けない。**

EASY / NORMAL / HARD / EXPERT / MASTER の全難易度を、同じ曲の1本のスコアランキングで競う。

ランキングは最終スコアそのものを比較する。

難易度上限スコアが

- EASY 600,000
- NORMAL 700,000
- HARD 800,000
- EXPERT 900,000
- MASTER 1,000,000

なので、MASTERを高精度で攻略できる上級者はより高いスコアを狙える。一方で、MASTERで大きく崩れたスコアより、低難易度を高精度・フルコンボしたスコアの方が上になる可能性がある。

難易度自体への追加順位補正は付けず、**あくまでスコアランキング**とする。

週間総合も1部門として扱い、難易度別ランキングへ分けない。

ただし、週間総合スコアを「3曲BESTの単純合算」にするか等の具体計算式はまだ確定していない。

---

## 20. 週間ランキング報酬（現時点の仕様・一部暫定）

週間の4部門それぞれで順位報酬を持つ方向。

### 上位報酬数

現在の作業基準:

| 順位 | 超越の実 | プシュケー |
| --- | ---: | ---: |
| 1位 | 5個 | 1,000 |
| 2位 | 4個 | 800 |
| 3位 | 3個 | 600 |
| 4位 | 2個 | 400 |
| 5位 | 1個 | 200 |

- 超越の実は1位5個から順位ごとに1個ずつ減らす
- プシュケーは1位1,000から順位ごとに200ずつ減らす
- **プシュケー数は暫定値**。本番実装前に経済バランスを再確認する

### 超越の実の種類

ユーザー確定事項:

- 「各ミッション」は対応している種族の **超越の実** を報酬にする
- **週間総合ランキング** は **虹の超越の実** を報酬にする

過去仕様との対応関係から、3曲側のランキング/ミッションでどの種族の超越の実を使うかは実装前に対応表を確認する。会話だけから勝手に種族を割り当てない。

### 週間参加報酬（暫定）

候補:

- ダイヤ ×3,000
- プシュケー ×50

参加報酬を「週全体で1回」にするか「部門ごと」にするか、参加成立条件を何にするかはまだ確定していない。前回会話での推測を正式仕様にしない。

---

## 21. 週間ミッション（存在は確定・詳細未確定）

週間システムにはランキングだけでなく週間ミッションも設ける。

確定している方向性:

- 週間ミッションを用意する
- 各ミッションは対応する種族の「超越の実」と結び付ける

現在復元できていないもの:

- ミッション一覧
- 必要回数
- クリア条件
- 各ミッションの報酬個数
- 全達成報酬
- ミッションと対象種族の対応表

以前の会話から復元できない場合は、ユーザーと改めて決める。勝手に作り直さない。

---

## 22. 週間システムの未確定事項

以下は現時点で未確定。実装時に必ず確認する。

- 週間リセットの曜日 / 時刻
- 集計期間の境界
- 同点順位の処理
- 週間総合スコアの具体的計算方法
- ランキング参加条件
- 各曲ランキングで使う超越の実の具体的種族対応
- 報酬受取方式
- 未受取報酬の翌週処理
- 参加報酬の成立条件と付与単位
- 週間ミッション一覧
- 週間ミッション必要回数
- 週間ミッション報酬個数
- 全達成報酬
- 永続全国ランキングの集計単位・難易度の扱い

未確定項目はコード・DBスキーマを先に作って固定しない。

---

## 23. 開発ロードマップ（現時点の合意）

大きな流れは以下。

1. **基礎の安定化**
   - レーン / ノーツ / 入力 / projection
   - TAP / HOLD / FLICK / SLIDE
   - 判定ライン
   - iPhone実機のタッチ・描画・タイミング
   - ここはPR #946時点で一旦固定して次へ進む
2. **譜面制作基盤の完成**
   - スマホ向け視覚エディタ
   - スナップ / ループ / 即テストプレイ / 不正配置確認
3. **あつ杯テーマの正式フル尺譜面**
   - EASY → NORMAL → HARD → EXPERT → MASTER
4. **正式音ゲーオプションの設計・実装**
5. **正式音ゲーHOME / 曲選択 / リザルト導線の設計・実装**
6. **自己ハイスコア表示の正式UI化**
7. **マスモン設定・ビート連動演出・経験値連携**
8. **複数曲の正式譜面を制作**
9. **永続全国ランキングの設計・実装**
10. **正式譜面が3曲以上揃った段階で週間システムを実装**
    - 毎週3曲
    - 3曲ランキング + 週間総合 = 4部門
    - 週間ミッション
    - 順位報酬
    - 参加報酬
    - リセット / 受取 / 同点処理
11. **演出・SE・コンボ・マスモン演出等の最終仕上げ**
12. **ユーザー明示許可後に正式公開**

順番は依存関係に応じて前後し得るが、週間システムを正式譜面3曲より先に公開・本実装しない。

---

## 24. 今後の保存ルール

週間システム、ランキング、報酬、ミッションなどを再確定した場合、会話だけに残さない。

少なくとも本書へ以下を保存する。

- 週間対象曲ルール
- 各曲ランキング
- 週間総合ランキング
- 難易度とスコアランキングの関係
- 週間ミッション
- 順位報酬
- ミッション報酬
- 参加報酬
- リセット
- 受取
- 未受取処理
- 同点処理
- マスモン連携
- オプション
- 正式HOME

「現在実装済み」「確定未実装」「暫定」「未確定」を混ぜない。

---

## 25. 参照履歴

主要PR:

- #862 音ゲー基盤 STEP 1
- #867 TAP実プレイ・ポーズ・リザルト・BEST
- #868 HOLD・複数指
- #884 FLICK・SLIDE
- #890 / #891 レーン・ノーツ・入力座標統一
- #900 レーン表示SVG統一
- #906 HOLD・SLIDE終端リリース判定
- #908 遠近感・投影位置再調整
- #910 HOLD・SLIDE終端ENDバー
- #919 仮ノーツヒットSE
- #920 HOLD・SLIDE正常終端SE
- #921 SLIDE 0.5レーン位置対応
- #934 境界越え入力・10サブレーン発光・空押し境界SE・判定表示自動消去
- #935 iPhone Touch接触幅入力の初期実装
- #937 押しっぱなし自動TAP対策・発光レイヤー調整
- #939 接触幅固定上限撤去・異常値guard
- #940 接触幅70% / 隣接25%基準
- #941 押しっぱなし中心deadzone / 指腹拡張追従
- #942 座標校正ガイド
- #943 MutationObserverフリーズ修正
- #944 譜面エディタ
- #945 EASY自動ドラフト実プレイ接続
- #946 判定ライン・実機タイミング補正・描画負荷調整
- #947 音ゲー現行仕様と週間ランキング計画を統合
- #948 視覚譜面エディタSTEP 1
- #949 HOLD終端の視覚伸縮
- #950 SLIDE経路の視覚編集
- #951 区間ループ
- #952 不正配置チェック
- #953 EASY音ハメ基礎監査
- #954 EASY正式候補v1（78ノーツ、TAP72/HOLD6、22点耳確認保留）
- #967 音ゲーオプション STEP1
- #970 保存済みオプションのruntime反映
- #971 画面構造の分離・ノーツ速度1.0〜12.0・HOLD/SLIDE表示geometry修正・レーン枠の多点projection化

この文書は会話ログの代替として使い、今後の音ゲー作業では最新mainと本設計書を確認してから変更する。
