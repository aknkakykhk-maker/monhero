# docs/refactor/ — 技術監査と基盤改善計画

2026-09-05 に `main` 9f9cfa0 を対象に行った全体監査の成果物。**コードは変更していない**(PHASE 1)。
新しいセッションで基盤改善を続けるときは、この順で読む。

| 文書 | 中身 | いつ読むか |
| --- | --- | --- |
| [`CURRENT_ARCHITECTURE.md`](CURRENT_ARCHITECTURE.md) | いまの構成(ファイル・行番号の地図、画面、保存、バトル、音声、音ゲー、検査、依存の向き)、actual / spec の差 | 最初。STEP 完了ごとに更新する |
| [`TECH_DEBT_AUDIT.md`](TECH_DEBT_AUDIT.md) | 負債 30 件(TD-01〜TD-30)と優先度、根拠の行番号、良い点 | 何を直すか決めるとき |
| [`REGRESSION_RISK_MAP.md`](REGRESSION_RISK_MAP.md) | 領域ごとの危険度、壊れ方、拾う検査、検査の穴、触らない領域 | **変更に着手する前に必ず** |
| [`TARGET_ARCHITECTURE.md`](TARGET_ARCHITECTURE.md) | 目指す形(連結ビルドで複数ファイル、保存はキーごとの更新関数、1 画面 1 コンポーネント、画面単位のライフサイクル) | 設計に迷ったとき |
| [`REFACTOR_MASTER_PLAN.md`](REFACTOR_MASTER_PLAN.md) | STEP 0〜10 の明細(目的・対象・変更内容・変更しないもの・依存・リスク・検査・完了条件)と着手順 | 次にやる PR を決めるとき |
| [`BATTLE_DAMAGE_MAP.md`](BATTLE_DAMAGE_MAP.md) | 予測ダメージと実ダメージの分岐の対応表と、一本化の形(STEP 5 の作業表) | STEP 5 に着手するとき |
| [`STEP6_HANDOVER.md`](STEP6_HANDOVER.md) | STEP 6 の引き継ぎ1枚。終わった画面・残った画面・この日に分かった型(props の洗い出し方、移してはいけないもの3つ、検査が静かに壊れること) | **STEP 6 を続けるとき最初に** |
| [`SCREEN_EFFECTS_MAP.md`](SCREEN_EFFECTS_MAP.md) | `setTimeout` 62 箇所を「画面専用 / 進行 / 対象外」に仕分けた表(STEP 6 の作業表)。移すときはここから引く | STEP 6 の画面切り出しに着手するとき |
| [`EXECUTION_PLAN.md`](EXECUTION_PLAN.md) | STEP ごとの担当モデル(Sonnet 5 / Opus 5)と effort、切り出し順、コピペ用の指示文 | **新しいチャットを始めるとき** |
| [`BASELINE_2026-09.md`](BASELINE_2026-09.md) | 変更前に全検査を回した結果(OK / NG の一覧と分類)。ここに無い NG が出たら「その変更で壊した」 | 検査が落ちたとき |

## 進捗

| STEP | 状態 | 備考 |
| --- | --- | --- |
| PHASE 1 監査 | 完了(2026-09-05) | 本フォルダ |
| STEP 0 文書同期 | 完了(2026-09-06) | D-01〜D-05・D-07 を解消。監査時点(9f9cfa0)以降に main は #1113 まで進み、モンビーの公開曲は 11 曲になっている |
| STEP 1 安全網 | 完了 | 1本目: 一括検査スクリプト `tools/run-checks.js` とベースライン(完了) / 2本目: エラー境界 `MhErrorBoundary`(完了) / 3本目: vm スタブ不足 11 本の修正(完了、NG 58 → 51) / 4本目: 残る 56 本を14体の読み取り専用エージェントで並列調査し、「検査の見かたが実装から取り残されている」「実データの書き間違い」「本当の不具合」の3つに仕分けて 46 本を修正(NG 56 → 12。PR #1273 / #1275) / 5本目: 残り12本のうち11本を修正(NG 12 → 1)。ランキング記録の700バイト枠、縦スクロール、潰れたプレイエリアの誤判定、長押しが死ぬ穴といった**本当の不具合**を直し、実ブラウザ検査7本(起動導線・Web Audio への移行・全難易度まとめ取り)を今の作りへ向け直した / 6本目: 最後の1本(`image/dye-edge-check`)も手作業なしで解決(**NG 0本**)。ウンディーネは見本マスクの塗り残しをいちばん近い部位で埋めて埋め込みマップを作り直し(輪郭22.89%→0.57%／内側2.18%→0%、データ量は据え置き)、あわせてモッチーの目を染色対象から外した(`tools/image/undine-region-rebuild.js` / `tools/image/mocchi-eye-unmask.js`)。内訳は [`BASELINE_2026-09.md`](BASELINE_2026-09.md) |
| STEP 2 連結ビルドと集約 | 完了 | 1本目: 編集元を `src/parts/` に分け、`game-system.jsx` を連結生成物にした(完了) / 2本目: 保存キー一覧を SAVE_DATA.md と突き合わせる検査(完了。49 個の未記載を補った) / 3本目: 残りを点検して完了にした(2026-09-11)。**定数・ユーティリティの集約は parts 分割で達成済み**だった——`BATTLE_SPEEDS` は `10-core.jsx`、`RANGE_LABELS` は `19-difficulties-and-rules.jsx` にあり、`wait` の重複定義も無い(4ファイルに見えたのは `waitUntilPlayable` / `waitForScreenOrientation` / ローカル変数)。`RHYTHM_SETTINGS_KEY` の二重定義も解消済み。`data-globals-check`(TD-20)は「`undefined-reference-check` が index.html の順に data を実行するので順序違反は読み込み時例外で拾える」として不要と再確認済み。完了条件の「難易度 ID 列を持つ tools 3 ファイルが定数を参照する」は、**そのまま実装から読ませてはいけない**と分かった——3本とも「検査が正解の並びを持つ」形で、実装から読むと自己一致になって意味が消える。かわりに `harness.readDifficultyIds()` を足し、唯一の穴だった `battle/battle-carousel-check`(既存9個の順序しか見ておらず、難易度を足しても落ちなかった)へ「実装の一覧と期待値が一致するか」を足した。架空の難易度 Mythic を差し込んで、落ちることを実測で確認した |
| STEP 3 保存層 | 着手中 | 1本目: 旧形式セーブの通し検査 `boot/legacy-save-boot-check.js`(完了) / 2本目: `mh_masu_mons` の state 更新と保存が対であることの検査 `masu/masu-save-pairing-check.js`(完了。現状は 35 箇所すべて対で、`saveTranscendFruitPair` のような「storeSet を注入し、読み戻して検証し、失敗なら巻き戻す取引関数」が既にある) / 3本目: 複数キー更新の正本 `saveStoredValuesOrRollback`(書く→読み戻す→食い違えば全部戻す)を置き、既存 2 本を包みにし、神殿の 6 箇所(合体・限界突破・超越・超越交換・転生・再生)を寄せた(完了。`masu/save-transaction-check.js` で固定) / 4本目以降: 残る複数キー更新(複数体合体・寄付・報酬受取)の寄せ、キーごとの読込関数 / 4本目: 主要6キー(`mh_gifts` / `mh_gold` / `mh_owned_items` / `mh_breeder_xp` / `mh_missions` / `mh_breeder_points`)の「state 更新と保存の対」を見張る `boot/save-state-pairing-check.js` を置いた(完了・2026-09-11)。**実測して本当の書き忘れは無かった**——未対に見えた5件は、起動時の読込2件(`setBreederXp(savedXp)` / `setGold(savedGold)`)と、取引関数の直後2件(`saveMarketBalances` / `persistSpeciesChallengeClearReward` が `storeSet` を注入)、その取引関数の中身1件で、いずれも正当な形だった。setter 64箇所 / storeSet 45箇所を固定。効くことは両方向で実測(保存のない setGold を1行足すと落ち、state 更新のない storeSet を足しても落ちる) / **次**: `mh_gifts` を読んだところ、計画にあった単独キーの `updateGifts()` は価値が薄いと分かった。4箇所のうち対になっているのは1箇所だけで、残りは「state が先・保存は条件付き」(起動時のログインボーナス)、「4キーまとめて保存」(ギフト受け取り)、「2キーを順序で担保」(ミッション→ギフト箱)。**寄せるなら単独キーの update ではなく、複数キーを `saveStoredValuesOrRollback` へ寄せるほうが効く**。とくにギフト受け取り(60-app.jsx:6915-6919)は gold/points/items/gifts の4キーを順に保存していて、途中で失敗すると部分適用になる / 5本目: **ギフト受け取りを取引へ寄せた**(完了・2026-09-12)。`claimGiftIds` は mh_gold / mh_breeder_points / mh_owned_items / mh_gifts を1件ずつ storeSet していて、途中で失敗すると「ダイヤは増えたのにアイテムが入っていない」片方だけの状態が残った。経験値が増えたときの mh_breeder_xp / mh_breeder_points_granted も含めて `saveStoredValuesOrRollback` の1取引へ。成立しなかったら画面の state を1つも更新せずに戻る。「配った総数」だけは state を持たないので、巻き戻す先を保存から読んでから取引に入れた。`boot/gift-claim-transaction-check.js` で固定し、**変更前のコードでは7項目すべてNGになる**ことを確認した |
| STEP 4 純関数の切り出し | 着手中 | 1本目: 共有層を節ごとに 21 部品へ分けた(移動のみ。完了) / 2本目: 純粋な部品 7 つを `parts.json` で `pure:true` と宣言し `boot/parts-purity-check.js` で守る(完了) / 3本目: `boot/parts-purity-check.js` を正規表現からBabelのスコープ解析へ切り替え、`19-difficulties-and-rules.jsx` を `pure:true` にした(完了・2026-09-11)。**この部品が pure になれなかった理由は `persistSpeciesChallengeClearRewardTransaction` ただ1つ**だったが、調べると `({..., storeSet, storeGet }) => ...` と**保存の道具を引数で受け取っていて**、本体も検査も自前の storeGet/storeSet を渡して呼んでいた——つまり「vm から追加のスタブ無しで呼べる」というねらいはすでに満たしていて、検査が名前の一致だけで弾いていた。移すのではなく検査を正した。効くことは実測で確認(グローバルの storeGet を1行足すと行番号付きで落ち、引数で受け取る形なら通る) / **残り**: 共有層21部品のうち8つが pure。残りは JSX・DOM・保存を本質的に含む部品で pure にする意味がない。`10-core.jsx` だけは3箇所(`React` の取り出し・`React.createElement` のSVGアイコン・`wait` の `setTimeout`)だが、いずれも土台の役割そのもの。**STEP 4 は実質完了**
| STEP 7 描画・キャッシュ | 着手中 | 1本目: 染め直した絵のキャッシュを 96 件の LRU に(完了。dataURL が無制限に溜まらない) / 2本目以降: 一覧行の `React.memo`、静的な `style={{}}` の定数化、CSS 静的化の準備 |
| STEP 5 バトル計算 | 完了 | バトル領域の古い検査 7 本を現在形へ(完了)。分岐の対応表 `BATTLE_DAMAGE_MAP.md`(完了)。1本目: 乱数固定の一致検査 `battle/damage-parity-check.js`(完了。2,560 通り一致) / 2本目: ヒット列を純粋な部品の `buildAttackHits` + `ATTACK_COMBO_RULES` に一本化し、実処理と予測をそこへ差し替え(完了) / 3本目: あつの挑発(`stun_atsu`)も `mainCanCrit:false` で同じ関数へ(完了。旧新を同じ乱数列で 72,000 通り比較して一致)。予測表示の既知差(確定会心時にあつの挑発のメイン 1 発ぶん多く出る)もユーザー指示で解消(完了) |
| STEP 6 画面の切り出し | 完了 | 1本目: 画面ライフサイクルの登録簿 `useScreenEffects`(`src/parts/40-screen-effects.jsx`)を置いた(完了)。`setTimeout` 62 箇所を1本ずつ読み、**画面専用 16 / 進行 31 / 対象外 15** に仕分けて `SCREEN_EFFECTS_MAP.md` に残した。進行(処理中フラグの戻し・次の画面へ進む・`await` の resolve)を止めると操作不能になるため、種別を宣言してから登録する形にし、書き忘れは「止めない」側へ倒す。`ui/screen-effects-check.js`(登録簿の振る舞いと分類表の突き合わせ)と `ui/screen-effects-browser-check.js`(本物の React での止まりかた)で固定。この本では登録簿を置くだけで、既存のタイマーは1本も移していない(ゲームの挙動は変わらない) / 2本目: `SETTINGS` を `src/parts/51-screen-settings.jsx` へ切り出した(完了)。props は「押されたら何をするか」を MonsterHeroGame 側に残して操作だけを渡す形にそろえた。この画面にタイマーは無いので登録簿はまだ使っていない。切り出し前後で JSX の骨格(要素・className・文言・並び)が完全一致することを機械比較で確かめ、実際に設定画面を開く経路(`boot/screen-error-boundary-check` の 設定→ヘルプ→デバッグ設定)も通した / 3本目: `MISSIONS` を `src/parts/52-screen-missions.jsx` へ切り出した(完了)。受け取り(`claimMission` / `claimMissionsBulk`)は保存とギフト送付を伴うので本体に残し、props で受ける。進捗の読み方(`normalizeMissions` ほか)は共有層の純関数なので画面から直接呼ぶ。タブの赤バッジ `tabCountBadge` はギフトボックスも使う小部品なので共有層(16)へ移した。`boot/mission-gift-badge-check` が本体の関数名を文字列で探していたため、「本体 → 画面の props → ボタン」の結線を見る形へ追随させた(緩めていない) / 4本目: `GIFT_BOX` を `src/parts/53-screen-gift-box.jsx` へ切り出した(完了)。受け取り(`claimGiftIds`)は保存を伴うので本体に残し props で受ける。受取可否・期限・報酬の読み方は共有層(17)の純関数なので画面から直接呼ぶ。3本目で共有層へ移した `tabCountBadge` をそのまま使えた / 5本目: `ITEM_INVENTORY` を `src/parts/54-screen-item-inventory.jsx` へ切り出した(完了)。戻り先がホームではなくプロフィールなので `onBack` で受け、画面は行き先を知らない。「使う」の対象えらびは本体側の別画面なので id を渡すだけ。**ここで STEP 6 共通の落とし穴が出た**: `60-app.jsx` を直接読む検査(12本以上ある)は、見ている画面が切り出された瞬間に対象を見失う。落ちればまだよく、「探す文字列がそもそも無い」形だと静かに対象外になる。`harness.js` に `readAppSource()`(本体＋切り出した画面をつないで返す)を置き、実際に落ちた `masu/soul-rank-step2-check` をそれで直した。**以降の画面で検査が落ちたら、まずこの関数へ差し替える** / 6本目: `BREEDER_MARKET` を `src/parts/55-screen-breeder-market.jsx` へ切り出した(完了)。購入と勇者の証での交換は保存を伴うので本体に残し props で受ける。所持判定(`isMarketItemOwned`)は本体の state を見るので判定ごと渡す。購入処理中は ref で持っているが、ref は変わっても描き直しが起きないので**props は真偽値**にした(描画のたびに読む今の作りと同じ)。助手の告知は更新履歴側の仕組みなので影響なし(`boot/market-notice-check` OK) / 7本目: `PROFILE` を `src/parts/56-screen-profile.jsx` へ切り出した(完了)。約19,500文字とこれまでで最大で、本体から受け取るものが38個。**props の洗い出しは `undefined-reference-check` にやらせた**(props を空にして仮組み → 落ちた40個をそのまま props にする)。手で数えるより速くて漏れがない / 8本目: 図鑑まわり3画面(`MONSTER_DEX` / `MONSTER_DEX_DETAIL` / `MONSTER_ATTACK_PREVIEW`)を `src/parts/57-screen-monster-dex.jsx` へまとめて切り出した(完了)。**図鑑専用の state 6個(絞り込み・選択中・タブなど)は本体に残した**——画面へ移すと、詳細から一覧へ戻ったときに絞り込みが消えて**挙動が変わる**ため。攻撃アクションの再生(コマ送りのタイマー)も本体に残し、画面からは呼ぶだけにした。`ui/screen-parts-check` は1ファイル複数コンポーネントを見る形へ広げた / 9本目: モンヒロビートの5画面(`RHYTHM_INFO` / 曲えらび / 案内 / マスモン枠 / 全国ランキング)を `src/parts/58-screen-rhythm.jsx` へ切り出した(完了)。**演奏画面(`RHYTHM_PLAY` / `RhythmTapTest`)は触っていない**(STEP 9 の領域・`REGRESSION_RISK_MAP.md` §4-1)。曲を決めて演奏へ入る処理は「指で押した直後」でないとブラウザに断られるため本体に残した / 10本目: `MASU_MONS`(マスモン一覧)を `src/parts/59-screen-masu-mons.jsx` へ切り出した(完了)。MASU_* 13画面のうち最小のものから型を作った。一覧を出すだけで保存に触らない / 11本目: 神殿の再生と寄付(`MASU_REGENERATION` / `_DETAIL` / `MASU_DONATION` と、同じ gameState にぶら下がる確認・結果の兄弟ブロック)を `60a-screen-masu-regen-donation.jsx` へ(完了)。**兄弟ブロックを置き去りにしない**のがこの領域のコツ / 12本目: 神殿の限界突破・転生・超越・魂格進化と、その演出・補償告知(12ブロック)を `62-screen-masu-temple.jsx` へ(完了)。**画面部品の名前は「数字-screen-…」にそろえること**——`60a-` のような付け方をすると `readAppSource()` が拾えず検査が静かに画面を見落とす(実際にそうなった。`ui/screen-parts-check` に命名規則の検査を足した) / 13本目: 魂格特性(`MASU_SOUL_TRAITS`・18千字)を `63-screen-masu-soul-traits.jsx` へ(完了)。`ui/screen-parts-check` の「setGameState を直接呼ばない」がここで初めて実際に働き、閉じる処理を props(`onClose`)へ寄せる漏れを1件拾った / 14本目: 超越強化(`MASU_TRANSCEND_ENHANCE`・31千字)を `64-screen-masu-transcend-enhance.jsx` へ(完了) / 15本目: マスモン強化(`MASU_ENHANCE`・18千字)を `65-screen-masu-enhance.jsx` へ(完了)。**切り出しでタイマーが画面へ移った初めての例**なので、`SCREEN_EFFECTS_MAP.md` の「現状」列に移動先を書き、`ui/screen-effects-check` が本体と切り出した画面を合わせて数えるようにした / 16本目: 合体(`MASU_FUSION`・38.9千字)を `66-screen-masu-fusion.jsx` へ(完了)。MASU系で最大。合体の実行は複数の保存キーを同時に書く取引なので本体に残し、画面は主・副えらび→確認→演出→結果の見せ方だけを持つ。魂格継承の目印(`data-soul-rank-inherit-fusion` / `-result`)を見ていた `masu/soul-rank-step5a-check` が `60-app.jsx` を直読みしていたので `readAppSource()` へ差し替えた。**これで `MASU_*` は `MASU_PATTERN_DEBUG`(デバッグ専用)を除いて完了** / 17本目: ラン開始まわりのえらぶ画面7つ(`SKIP_PICK` / スキップの最終確認 / `SKIP_RESULT` / `PICK_HERO`・`PICK_ALLY` / `PICK_PRO_ALLIES` / `PICK_SLOT` / `PICK_TEACHING`)を `67-screen-pick.jsx` へ(完了)。**進行フラグを戻すタイマーはすべてハンドラの中にあり本体へ残る**(画面の中に `setTimeout` は1つも無い)。勇者モンえらびと供モンえらびは元から1ブロックで 21 箇所ぶん `gameState===` で出し分けていたので、綴りだけを `pickMode`('hero' / 'ally')の props へ置き換えた。骨格の機械比較は綴りを元へ戻してから突き合わせている / 18本目: バトルの結果まわり7画面(`UPGRADE_SKILL` / `WAVE_RESULT` / `REWARD_PICK` と、ランの終わり方3つ(勝ち `CHAMPION` ・敗北 ・リタイア)、そこから開くマスモン登録)を `68-screen-run-result.jsx` へ(完了)。**ラン終了の処理(報酬の付与・記録の送信・保存)は本体に残し、画面は「もう決まった結果」を見せるだけにした**。敗北とリタイアは `gameState` を持たず `hp<=0` / `gaveUp` で出るが、同じマスモン登録へつながるので置き去りにせずまとめて移した。`battle/wave-result-layout-check` と `mode/species-challenge-auto-join-check` が `gameState` の位置から範囲を切り出していたので `screenSource()` へ差し替えた / 19本目: `HOME`(村の広場)と、そこにだけ重なる案内3つ(ききの加入・ももすけの登場・アップデートの案内)を `69-screen-home.jsx` へ(完了)。施設への7つの行き先は props(`onOpen*`)で受け取り、画面は行き先の名前を持たない。**HOME は配置の検査がある唯一の画面**なので `node tools/home-layout-check.js` を通して確かめた。`boot/kiki-intro-check` が「会話の始まりから次の画面のコメントまで」で JSX を切り出して実際に描いていたため、コンポーネント本体を描く形へ直した。`battle/battle-mode-check` の「バトル」ボタンの結線も、本体の行き先と画面の結線を2段で見る形へ / 20本目(最後): `BATTLE`(87千字)と、バトル中にだけ重なる演出4つを `71-screen-battle.jsx` へ(完了)。**A等級なので、計算・保存・ターン進行はひとつも移していない**——移したのは見せ方だけ。オート進行と演出のタイマーも本体に残り、画面の中に残る `setTimeout` はカードを置いたあと 0.5 秒で光を消す1つだけ。省エネ切替ボタンがバトル画面の中でもういちど `gameState==='BATTLE'` を見ていたので、`battleScreenActive` という props に置き換えた。`run/eco-mode-internal-check` と `run/quick-run-battle-band-check` が `gameState` の位置から範囲を切り出していたので `screenSource()` / `readAppSource()` へ差し替えた / **STEP 6 はこれで完了。`60-app.jsx` は 15,300行 → 13,800行。残るのは `processTurn` の `token.alive`(挙動の変更なので分けた)と `MASU_PATTERN_DEBUG`(デバッグ専用)の2つだけ。引き継ぎは [`STEP6_HANDOVER.md`](STEP6_HANDOVER.md) に1枚でまとめてある** |
| STEP 8〜10 | 未着手 | |

## 守ること(要約)

- 既存ゲーム・バランス・演出・UI・URL・`mh_*` キー・ランキング・画像・BGM を変えない。
- 全面書き直し・巨大 1 PR・仕様変更との混在・未使用に見えるコードの無断削除・既存 workflow のトリガー変更をしない。
- 検査を先に足し、落ちた検査を緩めない。本体を触る PR は `required` `ci` に加えて `boot` `battle` `mode` の領域まで回す(`BASELINE_2026-09.md` の教訓)。
- 音ゲーのタイミング基盤・保存の移行順序・ランキングの識別子は構造目的で触らない(`REGRESSION_RISK_MAP.md` §4)。
