name: Apply atsu cup theme once
on:
  push:
    branches:
      - chatgpt/atsu-cup-theme
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/atsu-cup-theme
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: tools/package-lock.json
      - name: Install locked tool dependencies
        working-directory: tools
        run: npm ci --omit=optional
      - name: Apply patch
        run: python tools/atsu-cup-theme-patch.py
      - name: Build
        run: node tools/build.js
      - name: Checks
        run: |
          node tools/audio/atsu-cup-theme-check.js
          node tools/audio/bgm-arrangement-check.js
          node tools/audio/bgm-check.js
          node tools/check-syntax.js
          node tools/undefined-reference-check.js
          node tools/update-notice-check.js
          node tools/build.js --check
          git diff --check
      - name: Remove temporary files
        run: rm -f tools/atsu-cup-theme-patch.py .github/workflows/apply-atsu-cup-theme-once.yml
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: add Atsu Cup theme BGM"
          git push origin HEAD:chatgpt/atsu-cup-theme
