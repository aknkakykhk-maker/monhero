# モンスターヒーロー開発ガイド

この文書は、人間と Claude・Codex・ChatGPT などの AI が共通して守る開発手順の正本です。
ゲームの概要と構成は [`docs/PROJECT_CONTEXT.md`](docs/PROJECT_CONTEXT.md)、未解決事項は
[`docs/KNOWN_ISSUES.md`](docs/KNOWN_ISSUES.md) を参照してください。Claude 固有の会話・公開ルールは
[`CLAUDE.md`](CLAUDE.md) も優先して確認します。

## 1. 基本原則

- ユーザーとの会話、進捗、最終報告は日本語で行う（コード、コマンド、ログは除く）。
- 依頼範囲を越えてゲーム仕様、バランス、セーブデータ形式、画像・音声を変更しない。
- 推測で仕様を補わず、既存コード・データ・履歴を確認する。不明点は質問または「未確認」と明記する。
- モバイルを主対象とし、縦画面、タッチ操作、通信量、初回表示、音声の自動再生制限を考慮する。
- 原則として `monster-hero/src/game-system.jsx` を編集し、自動生成物だけを直接編集しない。
- API キー、トークン、個人情報、端末固有のデータをコミットしない。

## 2. 着手前チェック

1. `git status --short --branch` でブランチと既存の変更を確認する。
2. `CLAUDE.md`、本書、`docs/PROJECT_CONTEXT.md`、`docs/KNOWN_ISSUES.md` を読む。
3. 関連コード、`tools/README.md`、直近の `git log` を調査する。
4. 受け入れ条件、変更対象、変更しない対象、検証方法を整理する。
5. 既存の未コミット変更は利用者の作業として扱い、無断で破棄・上書きしない。

## 3. 実装ルール

### ゲーム本体

- 配信元ソースは `monster-hero/src/game-system.jsx`。`monster-hero/game-system.compiled.js` は
  **`node tools/build.js` だけで生成する**。正規ビルド不能時は Bun や別の Babel などで代替生成せず、
  未完了として扱う。`src` だけが更新された状態を配信可能・完了扱いにせず、変更後は正規ビルドで
  生成物を同期して `node tools/build.js --check` を通す。
- モンスター・技などの定義は対応する `monster-hero/data/*.js` に置き、巨大な base64 画像は
  `monster-hero/data/images/` から不用意に移動しない。
- 起動経路、ローカル保存、ランキング通信、BGM/SE に影響する変更は、既存データとの互換性と
  iOS を含むモバイルブラウザの制約を確認する。
- 新しいBGMは、専用の自動再生条件を持つ曲も含め、特別な理由がない限り既存のBGMアレンジの
  共通選択肢へ登録し、選択・試聴・保存正規化まで確認する。
- タップ対象は十分な大きさを保ち、ホバーだけに依存しない。ノッチ、狭い画面、片手操作を意識する。
- 新しい依存関係や大容量アセットの追加は、必要性・容量・ライセンス・オフライン時の挙動を確認する。

### ドキュメント

- 同じ情報を複数箇所へ複製せず、正本へのリンクを置く。
- 現在の事実と将来の案を分ける。未着手の案を実装済みのように書かない。
- コマンドはリポジトリルート基準で記載し、変更時は関連文書も同じコミットで更新する。

## 4. 検証

変更範囲に応じて、次の順に実行します。スクリプトの詳細は [`tools/README.md`](tools/README.md) を参照してください。

```bash
# 初回のみ
cd tools && npm ci --omit=optional && cd ..

# 最低限の静的確認
node tools/check-syntax.js
node tools/build.js --check

# ゲーム機能を変更した場合の代表例
node tools/browser/feature-check.js
node tools/image/dye-report.js
```

`npm ci` が 403 になる場合は、まずエラーになった URL と `npm config list` を記録します。
Codex クラウドの通信ポリシーによる 403 は、レジストリ変更、認証情報の追加、Bun 等への置換で
回避しません。依存がないため正規ビルドできない場合でも、独立して安全性を確認できる編集元の実装と
コミットまでは行い、Codex 標準の Push / PR フローへ進めます。その PR は未同期の生成物を含むため
**配信可能・完了ではありません**。依存を利用できる環境で `node tools/build.js` を実行して生成物を同期し、
`node tools/build.js --check` が成功してから `main` へマージします。別の変換器で生成物を作ってはいけません。

ブラウザ検証には別ターミナルで `python3 tools/serve.py` を起動し、`http://localhost:8899/monster-hero/`
をスマートフォン相当のビューポートでも確認します。変更に対応する個別チェック（BGM、ランキング、戦闘など）を
優先し、実行できない検証は理由と未確認範囲を報告します。自動テストが通っても、利用者による実機・体感確認を
省略したとは扱いません。

## 5. バージョン更新と出荷

機能を公開する場合のみ、次の手順を行います。文書だけの変更ではバージョンや更新履歴を変更しません。

1. `node tools/build.js` で日時を揃え、配信用 JavaScript を生成する。
2. 関連テストと `node tools/build.js --check` を通す。
3. `BUILD_DATE`、`version.json`、更新履歴の最新リリース日時が同じであることを確認する（`node tools/build.js` が自動更新するため手入力しない）。
4. `monster-hero/data/changelog.js` の先頭へ利用者向け変更内容を追記してから再度ビルドする。
   新モード、新難易度、新機能、新モンスター、マーケット新商品、重要な仕様変更は更新履歴の対象とし、
   デバッグ限定変更や内部修正は掲載しない。新モード・主要新機能・マーケット新商品には `assistantNotice` も付け、
   `node tools/assistant/assistant-update-notice-check.js` で初回通知と遷移先を確認する。
5. **`monster-hero/data/help.js` のヘルプにも同じ変更を反映する**（更新履歴だけ書いてヘルプが古いままにしない）。
   一覧になるものは手書きせず `{ t:'data', id:'...' }` で実データから表を作り、画面を増やした場合は
   `HELP_SCREEN_COVERAGE` にも追加する。`node tools/help-coverage-check.js` /
   `node tools/help-guide-check.js` / `node tools/help-render-check.js` で更新漏れを検出できる。
6. 差分を再確認し、依頼された場合にコミット、プッシュ、PR 作成を行う。

GitHub Actions（`.github/workflows/compiled-check.yml`）は、**コミット済みの
`monster-hero/game-system.compiled.js` をそのまま GitHub Pages へ配信します**。
CI 側でビルドし直さないため、`node tools/build.js` を忘れたままコミットすると
`node tools/build.js --check` で落ち、公開もされません（配信物とリポジトリの中身を必ず一致させるための意図的な作りです）。

手元に Node 環境が無い状態（モバイルから GitHub の Web UI で編集した場合など）で
`node tools/build.js` を実行できないときは、Actions の
**ビルドと検査（手動実行）**（`.github/workflows/build-and-check.yml`）を対象の作業ブランチで
実行してください。生成物の再生成と、CI と同じ検査、差分があればコミットまで行います。
この用途のために使い捨てのワークフローを新しく作ることは禁止です（[`AGENTS.md`](AGENTS.md) 参照）。

## 6. Git / GitHub 運用

- 1コミットは1つの目的に絞り、例として `docs: add AI development guides` のような命令形の件名にする。
- `git diff --check` と `git status --short` で不要ファイルや空白エラーがないことを確認する。
- force push、履歴改変、他者の変更の取り消しは明示的な指示なしに行わない。
- PR には目的、主な変更、テスト結果、未確認事項、関連 Issue を記載する。
- ユーザーの指示と `CLAUDE.md` の運用ルールに従い、検証段階と公開段階を明確に区別する。

## 7. 完了条件

- 受け入れ条件を満たし、対象外の仕様を変えていない。
- 生成物とソースが一致し、必要な検証結果を記録した。
- 新たに判明した制約や不具合を `docs/KNOWN_ISSUES.md` に反映した。
- 変更ファイル、概要、検証コマンドと結果、残課題を日本語で報告した。

## 8. 追記テンプレート

新しい恒常ルールは次の形式で、この文書の適切な節へ統合してください。

```markdown
### ルール名

- 対象: <!-- ファイル、機能、環境 -->
- ルール: <!-- 必須事項 -->
- 理由: <!-- モバイル制約、互換性、運用上の背景 -->
- 検証: `実行コマンド` または確認手順
```

## 9. モンスター画像・円盤石・染色の標準フロー

- 対象: 新規モンスター追加、既存モンスター画像差し替え、円盤石画像、3色染色。
- ユーザーが実装したいモンスター画像を提示したら、その画像を元画像として扱う。元画像のデザイン・縦横比を勝手に変更しない。
- モンスター画像の実装前に、必要に応じて ChatGPT またはユーザー側で次の画像を用意する。Codex クラウドには画像生成・画像編集をさせない。
  1. 本体用の透過済みモンスター画像
  2. 円盤石用画像
  3. 3色染色用マスク画像
  4. 必要な場合のみブリーダーアイコン等の派生画像
- 透過済み本体画像は本物のアルファ透過 PNG とし、白背景・黒背景・チェッカー柄の焼き込み、輪郭の白フチ、色残り、半透明ゴミがないことを確認する。
- 染色マスクを作る前に、ユーザーと ChatGPT で必ず次を確定する。部位が未確定のままマスクを作らない。
  - 染色1の対象部位
  - 染色2の対象部位
  - 染色3の対象部位
  - 染色対象外の部位（目、白目、装飾など）
- 染色マスクの色ルールは固定する。
  - 赤 = 染色1
  - 緑 = 染色2
  - 青 = 染色3
  - 透明 = 染色対象外
- 画像を作成・編集したあとは、ChatGPT は必ずその画像について、ユーザーがそのままコピーできる形で「ファイル名」「GitHub 上の保存先」「用途」をセットで提示する。ファイル名・保存先は最新 main の既存命名規則と配置を確認して決め、推測で新しい規則を作らない。
- ユーザーが画像を GitHub に保存した後、Codex / Claude には保存済み画像を確認してコードへ反映させる。画像生成・画像編集・マスクの描き直しは依頼しない。
- 3色染色の実装では、保存済みの `*-dye-mask.PNG`（配信しないので `tools/art-sources/dye-masks/` に置く）を「部位分けの正解見本」として扱う。ユーザーから別途指示がない限り、ゲーム実行時にそのマスク PNG を直接読み込ませる新方式へ変更するのではなく、既存の染色システムを再利用し、デバッグ染色結果を正解マスクの赤・緑・青の境界へ合わせる。
- 染色実装の完了前に、少なくとも「染色1のみ」「染色2のみ」「染色3のみ」「3色同時」を確認する。髪と顔、髪と腕、首、服と肌、腰と尻尾、細い先端、髪の下に隠れた腕など、境界の崩れや色漏れが出やすい箇所を重点確認する。
- `dye-region-map` 等の自動出力や数値チェックが成功しただけでは、見た目まで確認済みとは扱わない。可能な環境では確認画像を正解マスクと見比べ、実機確認が未実施ならその旨を明記する。
- 画像や染色の修正指示を作り直す場合は、差分追記ではなく、その時点の確定内容を反映した完成版指示書を毎回提示する。

### 更新バナー・更新情報の必須確認

プレイヤー向けの機能追加・仕様変更・不具合修正を公開する変更では、`monster-hero/data/changelog.js` の先頭へ更新情報を追加してから通常ビルドを実行し、`BUILD_DATE`・`monster-hero/version.json`・最新更新日時を同期させる。`node tools/update-notice-check.js` を必ず通し、上部の新バージョン通知（更新バナー）と「更新履歴」の両方へ届く状態を確認する。更新情報を出さないことが明示された内部変更だけを例外とする。

#### 開発中の作業メモは `dev: true` を付ける

`releaseFlag` は「出す時期」を遅らせるだけで、公開フラグを `true` にした瞬間にそれまで書いた項目が
**まとめて**更新情報へ並ぶ。モンヒロビートのプレオープンで実際にこれが起き、公開前に書いた80件が
一度に表へ出た（2026-09-05・ユーザー指摘「デバッグの内容が一気に更新情報に来たから消して」）。
プレイヤーから見れば、触ったこともない画面の不具合が直った話が延々と続くだけになる。

公開前の機能について書くときは、次で使い分ける。

| 書く内容 | 付ける印 |
| --- | --- |
| プレイヤーが公開後に体験する内容 | `releaseFlag` だけ（公開と同時に出る） |
| 作っている途中の調整・内部の作り直し・まだ誰も見ていない不具合の修正 | `dev: true` も付ける（ずっと出さない） |

迷ったら「公開初日に遊ぶ人がこれを読んで意味が分かるか」で決める。
`dev: true` の項目は更新情報・不具合情報のどちらにも出ず、助手の告知にもならない（記録としては残る）。
判定は `game-system.jsx` の `changelogForPlayers` 1か所。
`node tools/changelog/dev-entry-check.js` で、出していないこと・付け忘れが無いことを確認する。

### 画面に出る文字を検査の目印にしない

DOM を外から掴む処理（iPhone 向けの `touchend` 橋渡しなど）で、
**ボタンの表示テキストで「どのボタンか」を見分けてはいけない。**
`data-*` 属性を付け、そちらで見分ける。

2026-09-05 に、ポーズの「中断して音ゲーデバッグへ戻る」を「曲えらびへ戻る」へ
書き換えた結果、文字で見分けていた橋渡しが外れ、iPhone でポーズの戻るが
**完全に無反応**になった。文字を変えたのは別の指摘に応えるためで、
橋渡しを壊した自覚はまったく無かった。

同じ理由で、**検査の側も「その文字列が書いてあること」を条件にしない。**
このときの検査（`rhythm-mode-restart-remount-check.js`）は
「文言で見分けていること」を要求していたため、文言を変えた瞬間に
検査ごと前提が崩れ、壊れたことを誰も検出できなかった。
振る舞いを守りたいなら、`tools/mode/rhythm-pause-bridge-check.js` のように
**実際に動かして結果を見る**。`monster-hero/vendor/` に React があるので、
本物の React と本物のブラウザイベントで確かめられる。

外から掴む処理を書くときは、あわせて
**「見分けられなかったときは何もしない」** を必ず入れる。
上の不具合が「押しても無反応」まで悪化したのは、見分けから外れたボタンにも
印を付けてしまい、そのあとの本物の `click` を自分で潰していたためだった。

### 測った寸法を「0でないから正しい」と決めつけない

画面の寸法を測って覚える処理では、**覚える条件を「0でないこと」にしない。**
まだ組み上がっていない最中の DOM は、0 ではないが正しくもない値を返す。
実測では、844px の画面でプレイエリアが 3352px あった。

2026-09-05 に、モンビーがこの値を覚え込んで固定してしまい、
ノーツが画面の外に置かれたまま MISS だけが積み上がる不具合を出した。
リスタートで直っていたのは、そのとき覚えた値を捨てていたからにすぎない。

覚えるときは「その値で目的の動作ができる形になっているか」を確かめる
（モンビーでは `rhythmTravelLooksReady`）。あわせて次を守る。

- **寸法の変化は `ResizeObserver` で拾う。** 絵の読み込み完了・全画面・
  セーフエリアの確定は `window.resize` を起こさないことがあり、取りこぼす
- **画面が組み上がる前に、時間の進む処理（音の再生など）を始めない**
- **画面に出せていないものを、出したことにして減点しない**

### 「押せるか」を測るときは、構えられるかどうかまで見る

指が届く範囲を測るとき、**「その場所へ事前に寄せて構えられるか」を必ず考える。**
理屈の上では指が入る配置でも、次に何が来るか分からなければ端へは寄せられない。

2026-09-05、押さえている指が「帯の端から端まで好きな場所を押せる」前提で
測っていたため、物理的に押せない配置が生成でも検査でも素通りし、
先行公開5曲に163か所残っていた。

- 押さえ続けるもの（HOLD/SLIDE）と、叩くだけのもの（TAP/FLICK）は**別の物差し**にする
- 経路が決まっているもの（SLIDE）は、その経路から動かせない
- 物差しは1か所（`tools/mode/rhythm-hand-model.js`）に置き、
  生成・検査・自動修正がすべてそこを見る。道具ごとに書くと、直したつもりで別物になる

### 迷う仕様は「言い分の表」を作ってから決める

同じ場所を何度も直していて、片方を直すともう片方が壊れるときは、
**これまでの言い分をすべて1つの表にしてから**決める。

2026-09-05 のタップ判定はこれで決着した。3回の直しでそれぞれ別の不具合が出ており、
4案目でようやく全部が同時に立った。表は検査
（`tools/mode/rhythm-tap-target-check.js`）にそのまま書いてあるので、
次に触る人は「どれを壊してはいけないか」を最初に読める。

### 同じ操作でも「順番」が違うやり方を数えておく

プレイヤーの操作を受け取る処理では、**同じ目的でも順番の違うやり方が複数ある**ことを
最初に数える。片方だけ実装すると、もう片方でしか遊ばない人には完全に壊れて見える。

2026-09-05、長押し・スライドの指の置き換えで
「離してから置き直す」だけを実装しており、
「押さえたまま2本目を置いてから離す」（親指で遊ぶ人の基本操作）が必ずMISSになっていた。
どちらも同じ「持ち替え」なのに、順番が逆というだけで一方が丸ごと抜けていた。

- 実装する前に、その操作のやり方を**全部書き出す**
- 検査には**全部のやり方を並べて書く**。1つだけ通っても意味がない

### 画像を書き出す道具には減色を必ず入れる

画像を作り直す道具は、**流すたびに配信物が重くならないこと**を確かめてから使う。

2026-09-05、助手の顔アイコンを作り直したところ、1枚 43KB だったものが 121KB になった。
`canvas` の `toBuffer` は RGBA のまま書き出すためで、以前のファイルは別の工程で
減色（パレットPNG）されていた。道具にその工程が入っていなかったので、
**誰が流しても3倍に太る**状態が残っていた。

- 書き出しは減色（`sharp().png({palette:true})`）を通す
- 道具を流したあとは `git diff --stat` でファイルサイズの増減を必ず見る
- 表示が小さい画像（アイコン・吹き出しの顔）は256色で十分。見た目は変わらない

### 絵の形式は「透明が要るか」で選ぶ（PNGのままにしない）

配信する絵は、**透明部分が要るかどうか**で形式を決める。要らない絵をPNGのまま置くと、
同じ見た目で何倍も重いファイルを毎回配ることになる。

2026-09-05、タイトル画面の絵が 2.5MB のPNGだった。941x1672 の一枚絵で透明部分は無く、
文字も入っていない（文字はDOM側で描いている）。JPEG（品質92・色差の間引き無し）へ
置き換えたところ 0.67MB になり、**起動時に読み込む量が 7.2MB → 5.3MB** へ減った。
200%に拡大して並べても差は分からない。

- 透明が要る → PNG。要らない → JPEG（写真・イラストとも）
- 置き換える前に「元と並べて拡大した画像」を作って必ず目で見る。
  誤差の平均だけを見て決めない（文字やロゴが入っていると平均は小さいのに輪郭だけ崩れる）
- 逆に、**既にあるPNGを再圧縮しても縮むとは限らない**。この配信物の `images/` は
  すでに最適化済みで、`sharp` で入れ直すと 30% 太った。**縮むことを実測してから触る**

置き換えた古いファイルは、**1回公開を挟んでから消す**。GitHub Pages は index.html にも
10分のキャッシュを返すため、古い index.html を持ったままの人が消したファイルを取りに行く。
タイトル画像は読み込みに失敗すると起動が止まる作りなので、そこで詰まる。
`monster-hero/data/images/title-screen-clean.PNG` は 2026-09-05 に参照を外しただけで
残してあり、**次の公開のときに消してよい**。

### 起動でいちばん大きいファイルから先に取りはじめる

ローディングの棒は「受け取ったバイト数 ÷ 全体」で動くが、
バイト単位で数えられるのは `__mhBoot.prefetch()` を通したファイルだけで、
ほかは読み終わった瞬間に一段跳ぶ。だから**いちばん大きいファイルを先読みに入れていないと、
そこで棒が長く止まって見える**。

2026-09-05にタイトル画像を軽くした結果、いちばん大きいファイルが本体JS（約2.7MB）に変わった。
本体JSは `<script>` でいちばん後ろから読むので、棒が最後で止まる形になっていた。
先読みへ入れて直した（`tools/boot/boot-progress-check.js` の描き直し回数が 43回 → 46回）。

先読みと `<script>`／`<img>` は**同じURL**にする。ずれると同じファイルを2回取りに行く。
本体JSは `?v=GAME_BUILD` が付くので、`GAME_BUILD` を先読みより前で宣言して両方で使う
（`tools/stamp-version.js` は `var GAME_BUILD = '...'` の**最初の1つ**を書き換えるので、
literal を上へ移し、下は `window.__mhGameBuild` を読むだけにしてある）。

### playwright の `route()` はブラウザのキャッシュを切る

`page.route()` を1つでも登録すると、そのページはHTTPキャッシュを使わなくなる。
**「同じファイルを2回取りに行っている」ように見えても、検査の副作用のことがある。**

2026-09-05、先読みしたファイルが `<script>`／`<img>` から取り直されているように見え、
`fetch`(cors) と `<script>`(no-cors) でキャッシュの鍵が違うのだと考えかけた。
`route()`（Tailwind CDNを落とすために入れていた）を外して測り直すと、
どちらも**1回だけ**だった。通信の回数を測るときは `route()` を外して確かめる。

### タブの振り分けを変えるときは「既読」が移ることまで見る

一覧をタブで分けている画面では、既読の状態も**タブごとに**保存していることが多い。
どの項目をどちらのタブへ出すかを変えると、前に読んだ項目のIDが別のタブへ移る。
このとき次の2つが両方そろっていないと、**既読が未読(NEW)へ戻る**。

- 未読の判定を、そのタブの既読だけでなく**全タブの既読の合計**で行う
- 既読を保存するとき、いま出ているIDで**上書きしない**（残っているIDへ足す）。
  消えた項目のIDは「いま存在するID全部」でふるいにかければたまり続けない

2026-09-05、不具合修正(fix)を「更新情報」から「不具合情報」へ移した。
移すだけだと、13件の既読が不具合情報タブでNEWに戻る状態だった。
`tools/changelog/seen-across-tabs-check.js` が、旧ビルドの保存の形を実際に作って
読み込み直し、NEWが出ないことを本物のブラウザで確かめる。

あわせて、**「直近N件」を前提にした検査は書かない**。
種類の札の検査が「直近5件に2種類以上ある」を見ていたため、
タブを分けた結果たまたま同じ種類が続いただけで落ちた。
並び順ではなく一覧全体を見るようにした（サンプルを広げているので、緩めてはいない）。

### 遊ぶのに要る形は、外部CDNのCSS(Tailwind)に任せない

Tailwind は外部CDNのJITが**あとから**CSSを作る。作られるまでのあいだ、
`bottom-[12%]` も `h-[3px]` も `bg-gradient-to-r` も `relative` も効かない。
つまり **「高さ0・背景なし・位置の基準なし」の要素が一瞬(端末や回線によっては数秒)出る**。

2026-09-05、実機で「演奏を始めたときに下部の判定ラインがないときがある」と報告された。
判定ラインは位置・厚み・色をすべてTailwindのクラスで書いていたため、
CSSが間に合わないあいだは見えず、しかもプレイエリアの `relative` も効かないので
置き場所の基準まで別の要素へ移り、線が画面の上端へ出ていた
(検査で実測: 修正前はプレイエリアの上から0%、修正後は87%)。

- **遊ぶのに要るもの**(判定ライン・プレイエリアの `position`/`overflow`)は
  JSXの `style` へ直接書く。見た目の飾りはTailwindのままでよい
- 「出ているか」を確かめる検査は、**Tailwindを与えない状態**で走らせる。
  このサンドボックスはCDNへ出られないので、そのまま
  「Tailwindが最後まで来なかったいちばん悪い場合」の検査になる
  (`tools/mode/rhythm-start-sequence-check.js`)
- 「整ったか」を見る判定は、**大きさと位置の両方**を見る。
  以前は「線の中心がプレイエリアの中にあること」しか見ておらず、
  高さ0のまま先頭に居る状態を通していた

### 演奏は「押した瞬間」ではなく、構える時間を置いてから始める

音ゲーで曲えらびの決定と同時に曲が鳴ると、指を置く場所を決める間が無い。
2026-09-05にユーザーから「入ってすぐ音楽なるのも良くない？ 3秒から5秒ぐらいしてから
演奏がいい」と指摘され、READY→3→2→1(各0.8秒・計3.2秒)を挟むようにした。

この待ちには副作用として「レイアウトが完全に固まる時間」という役目もある。
`RHYTHM_COUNTDOWN_*` は `data/rhythm-mode.js` に置いてあるので、
長さを変えるときはそこだけを触る。

### `const` の定義は「使う場所より前」に置く

`useCallback` の依存配列に、まだ定義していない `const` を書くと
**初期化前アクセスで画面が真っ白**になる。構文も参照先も正しいので
`check-syntax.js` も `undefined-reference-check.js` も通ってしまう。

2026-09-05、`disposeRun` から `clearCountdown` を呼ぶようにしたとき、
`clearCountdown` を `disposeRun` より後ろに書いてモンビーの演奏画面が真っ白になった。
`render-error-check.js` はタイトル画面しか開かないので、これも拾えなかった。

- 片付け用の関数は、それを呼ぶ関数より**必ず上**に書く
- **画面を実際に開く検査を、その画面ぶん用意する**。
  タイトルが出ることは、奥の画面が出ることの保証にならない

### 丸枠のアイコンは「元絵の中で被写体がどこにいるか」から決める

丸枠での見え方は `MARKET_PROFILE_ICON_STYLES` の `scale`/`x`/`y` だけで決まる。
値を勘で動かすと、直したつもりで別のキャラとそろわなくなる。
**元絵の中で顔がどこにあるかを測ってから決める。**

2026-09-05、実機で3件を指摘された。

| | 症状 | 原因 | 直し方 |
| --- | --- | --- | --- |
| アーク | 右に寄っている | 元絵の時点で顔が右へ8.3%ずれている（目の中心が画像の58.3%） | `x` で打ち消す。9.0% → 1.4% |
| イブリース | 近すぎる | 頭上のボールを枠外へ出すために倍率を上げていた | 引いてボールごと入れる（角と光輪が丸ごと入る位置） |
| パンドラ | 少し遠い | 立ち絵をそのまま使っており顔が小さい | 角と光輪が切れない範囲まで寄せる |

- **顔の中心は左右対称のパーツ（両目）の重心で測る**。輪郭の重心だと、
  片側だけ伸びた翼や髪に引っ張られて中心がずれる（アークがまさにこれ）
- **横へ寄せるときは倍率の下限がある**。元絵がふちまで描かれている画像は、
  寄せたぶんを覆えるだけ拡大しないと、画像の直線のふちが枠内へ出て
  切り貼りしたように見える（`x=-8.3` なら `scale>=1.166`）
- 検査は `tools/image/market-icon-framing-check.js`。
  元画像を実際に丸枠へ描いて測るので、値を触れば必ず結果が変わる
