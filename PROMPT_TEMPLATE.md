# AI 作業依頼テンプレート

Claude・Codex・ChatGPT へ作業を依頼するときにコピーして使うテンプレートです。
空欄を具体化し、不要な節は削除してください。新規チャット全体の開始方法は
[`START_NEW_CODEX_CHAT.md`](START_NEW_CODEX_CHAT.md) を参照してください。

## 標準テンプレート

```markdown
# 依頼: <短い作業名>

## 背景・目的
<!-- 利用者が困っていること、なぜ必要か。 -->

## 対象
- 機能 / 画面: <例: バトル結果画面>
- 主なファイル候補: <不明なら「AI が調査」>
- 対象端末: <例: iPhone Safari / Android Chrome / 全端末>

## 受け入れ条件
- [ ] <利用者から見た完了条件 1>
- [ ] <完了条件 2>
- [ ] 縦画面・タッチ操作で既存機能が後退しない

## 変更しないもの
- <ゲームバランス、保存形式、画像、音声など>

## 再現手順 / 現在の状態
1. <画面を開く>
2. <操作する>
3. <現在の結果>

期待結果: <本来の結果>

## 参考情報
- 関連 Issue / PR: <#番号またはなし>
- スクリーンショット / ログ: <場所またはなし>
- `KNOWN_ISSUES.md` の項目: <KI-NNN またはなし>

## 検証
- 必須: `<実行してほしいコマンド>`
- 手動: <画面、操作、端末条件>
- 実行できない確認は、理由と未確認範囲を報告すること

## Git / 納品
- ブランチ: <現在のブランチ / 新規ブランチ名>
- コミット: <必要 / 不要>
- プッシュ: <必要 / 不要>
- PR: <必要 / 不要、ベースブランチ>
- 最終報告: 変更ファイル、概要、テスト結果、残課題を日本語で記載
```

## 不具合修正向け追加項目

```markdown
## 不具合情報
- 発生頻度: 常に / ときどき / 1回のみ
- 初回確認日: YYYY-MM-DD
- 環境: 端末 / OS / ブラウザ / 画面サイズ
- セーブ状態: 新規 / 既存 / 引き継ぎ後
- 回避策: あり（内容） / なし / 不明
- コンソールログ: <!-- 秘密情報を除いて貼る -->
- 回帰テスト: 同じ不具合を検出するチェックを追加する / 理由を示して追加しない
```

## UI・演出変更向け追加項目

```markdown
## UI 要件
- 基準画面: <スクリーンショットまたは既存画面>
- 縦画面の最小幅: <例: 320px>
- タップ領域: <大きさ、誤操作防止>
- セーフエリア: <ノッチ / ホームインジケータへの配慮>
- アニメーション: <時間、無効化時の挙動>
- 音声: <ユーザー操作、自動再生制限、消音時の挙動>
- アクセシビリティ: <色以外の識別、文字サイズ、ラベル>
```

## AI への共通指示（必要に応じて添付）

```markdown
作業前に `CLAUDE.md`、`DEVELOPMENT.md`、`PROJECT_CONTEXT.md`、
`KNOWN_ISSUES.md` と関連コードを読んでください。既存の未コミット変更を保持し、
推測でゲーム仕様を変更しないでください。最小限の差分で実装し、変更範囲に対応する
`tools/` のチェックを実行してください。新しい制約や未解決事項を発見した場合は、
`KNOWN_ISSUES.md` の更新案を示してください。報告は日本語で行ってください。
```
