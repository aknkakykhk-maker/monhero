# リポジトリ共通の開発ルール

このファイルは、このリポジトリ全体で作業する AI エージェントに適用します。詳細な手順は
[`DEVELOPMENT.md`](DEVELOPMENT.md)、プロジェクトの前提は [`docs/PROJECT_CONTEXT.md`](docs/PROJECT_CONTEXT.md) を正本とします。

## 作業の開始と範囲

- 作業開始時に、この `AGENTS.md`、[`README.md`](README.md)、[`docs/PROJECT_CONTEXT.md`](docs/PROJECT_CONTEXT.md) を読む。続いて `DEVELOPMENT.md` と変更箇所に適用される下位の `AGENTS.md`、関連文書を確認する。
- `git status --short --branch` で既存変更を確認し、利用者の変更を破棄・上書きしない。
- 依頼範囲外のコード、仕様、データ、アセット、文書を「ついでに」変更しない。不明な要件は推測せず、質問するか未確認事項として明記する。
- ゲーム仕様・バランス、ランキング、Supabase のスキーマや通信、セーブデータ形式は、明示的な依頼と互換性・移行方針なしに変更しない。
- **既存データを壊さない**。保存キー(`mh_*`)の改名・削除・意味の変更、セーブデータやランキングの削除・初期化は行わない。
  保存項目を増やすときは新しいキーを足し、読み込みは必ず既定値へ落ちる正規化を通す。一度きりの移行には専用フラグを持たせて二重適用を防ぐ。
- 機能を追加・変更したときは、`monster-hero/data/changelog.js`（更新履歴）と `monster-hero/data/help.js`（ヘルプ）の
  両方を必ず更新する。ヘルプの更新漏れは `node tools/help-coverage-check.js` で検出できる。
- 新モード・主要新機能・マーケット新商品は、更新履歴に加えて `assistantNotice` による助手の初回通知まで確認し、`node tools/assistant/assistant-update-notice-check.js` を実行する。

## GitHub Actions・ブランチ・PR の運用

ChatGPT・Codex・Claude Code のどれで作業しても、ここは同じ手順にする。

### ワークフローは2つだけ。増やさない

- 置いてよいのは次の2つだけとする。
  - `.github/workflows/compiled-check.yml` … PRとmainのCI・GitHub Pages公開。**検査を減らさない**
  - `.github/workflows/build-and-check.yml` … 手元にNode環境が無いときにビルドと検査を回す手動実行(`workflow_dispatch`)
- **変更のたびに新しいymlを作らない。** 「1回だけパッチを当てる」ための使い捨てワークフロー
  (`*-once.yml` / `_tmp_*.yml` / `apply-*.yml` など)を追加しない。
- ワークフローが失敗したら、別名のymlを作り直さない。同じワークフローを直すか、押し直す。
- ワークフロー自身に `git commit` と `git push` をさせるのは `build-and-check.yml` の生成物コミットだけにする。
  `on: push` で起動して自分でpushする構成は、起動が連鎖して回数が読めなくなるため作らない。
- **既存の2つのワークフローの `on:` 条件を一時的に書き換えて使わない。** 新しいymlを作らずに
  `build-and-check.yml` を `workflow_dispatch` から `push` へ変えてpushで連打し、
  終わったら書き戻す、という使い方も禁止する。新しいファイルを作っていないだけで、
  やっていることは使い捨てワークフローと同じ(pushで連鎖起動させる)。
  ビルドが必要なときは、書き換えず Actions 画面の Run workflow から手動で起動する。

> ⚠️ 2026年8月に、この形の使い捨てワークフローが2日で41個作られ、登録ワークフローが42個、
> 画面上の変更1件あたり8 Runまで膨らんだ。うち6 Runが使い捨て側で、失敗するたびに
> 別名のymlを作り直していたことが原因だった。同じ作り方を繰り返さない。
> 対策後も、`build-and-check.yml` の `on:` を一時的に `push` へ書き換えて連打し、
> 完了後に書き戻すという形で同じことが再発した。ファイルを増やさなくても、
> 起動条件を書き換えれば同じ問題が起きる。

### モバイルから編集するとき

- Web UIでソースだけを編集すると、配信用の `game-system.compiled.js` が古いまま残る。
  そのままではCIの `node tools/build.js --check` で必ず落ちる。
- そのときは Actions → **ビルドと検査（手動実行）** → Run workflow を、対象の作業ブランチで実行する。
  生成物の再生成とコミットまで自動で行う。新しいワークフローを書く必要はない。
- ビルドと検査が必要な変更は、Node環境を持つ Claude Code で行ってもよい。

### ブランチ

- 作業ブランチはマージまたはクローズしたら削除する。放置しない。
- どのツールの作業か分かる接頭辞を付ける(`claude/` / `chatgpt/` / `codex-` / `work/`)。
- 同じ目的で `-v2` `-v3` と作り直さない。既存の作業ブランチを直す。

### PR

- PRは作った日のうちに、マージするかクローズするかを決める。
- 古いPRは「古いから」で閉じない。現在のmainに同じ実装があるかを確認してから判断する。
- 古いPRをそのままマージしない。baseが古いと、その後mainへ入った変更を巻き戻す。
  必要な機能が残っている場合は、現在のmainを基点に作り直す。

### マージがコンフリクトで断られたとき

**それは作業の終わりではない。** GitHubが `Pull Request has merge conflicts` を返しても、
そこで報告して止まらない。**解消してマージし切るまでが1つの作業**であって、
ユーザーへ声をかけて判断を仰ぐ場面ではない。

> ⚠️ 2026-09-13にユーザー指摘。「マージ出来なかった場合、そのままこっちから声かけないと
> そのまま放置のときあるよね？」。作業が終わった気になっているところへ**外部要因のエラー**が
> 来るので、「報告して止まってよい場面」に見えてしまうのが原因。見えるだけで、そうではない。
> 放置されたPRは、次に誰かが気づくまでmainへ入らない。

**まず起きないようにする。** PRを作る直前に `git fetch origin main` して、進んでいたら
取り込んでおく。これだけでほとんど起きなくなる（残るのは、その数十秒の間に
別の作業がマージしたときだけ）。

**起きたときの手順。** ぶつかるのはほぼ毎回**ビルドの生成物6つ**で、中身の競合ではない。
セッションを2つ同時に走らせていると、直している場所がまったく別でも必ずぶつかる。

```
git fetch origin main && git merge origin/main
# 生成物は main 側を取る（このあと作り直すので中身は何でもよい）
git checkout --theirs monster-hero/game-system.compiled.js monster-hero/index.html \
  monster-hero/src/game-system.jsx monster-hero/src/parts/10-core.jsx \
  monster-hero/tailwind.css monster-hero/version.json
git add （上の6つ）
# 中身の競合が残っていないか。残っていたらそこだけ手で直す
git diff --name-only --diff-filter=U
#   ★更新履歴(monster-hero/data/changelog.js)は「先頭に足す」規則なので、
#     同じ日に両方が項目を足すと本当にぶつかる。両方の項目を残し、新しい順に並べる
# ビルド日時を「いまの実時刻(JST)」に合わせて作り直す
TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M'   # ← 10-core.jsx の BUILD_DATE と version.json の両方へ
node tools/build.js && node tools/build.js --check
# 検査を通してからコミット・プッシュ・マージ
```

## Codex 作業ルール

- 依頼された実装を行う。
- 必要なビルドとテストを行う。
- 変更がある場合は必ずコミットする。
- コミット後は作業ツリーをクリーンにする。
- push および PR 作成は Codex アプリの標準フローに任せる。
- remote、`main`、作業ブランチを独自に作成、変更、改名しない。
- remote や push の状態確認を理由に、実装やコミット前で作業を止めない。
- PR メタデータを手動作成しない。
- 直接 `git push` を実行できなくても異常扱いにしない。
- コミット完了後、Codex アプリ側でプッシュ可能な状態にする。
- 変更がない場合のみコミット不要とする。
- Codex クラウドで npm レジストリが 403 となり正規ビルド不能でも、安全に確認できる編集元の実装・静的確認・コミットまでは完了し、Codex 標準の Push / PR フローへ進める状態にする。ただし `monster-hero/src/game-system.jsx` と配信用生成物が未同期なら「配信可能」「完了」と報告せず、PR の未確認事項へ明記する。
- `main` へマージする前に、依存を利用できる環境で `node tools/build.js` と `node tools/build.js --check` を必ず成功させる。Bun 等の別変換器による代替生成は禁止する。

### 大容量ファイルの読み込み

- トークン消費を抑えつつ調査精度を維持するため、大容量ファイルや行数の多いファイルを最初から全文読み込みしない。`cat` 等による無条件の全文表示は避ける。
- ファイルサイズや行数が不明な場合は、`ls -lh`、`stat`、`wc -l` 等で先に確認する。
- 調査対象が分かっている場合は `rg` / `grep` のキーワード検索から始め、`find`、`head`、`tail`、`sed`、`awk`、Python 等で必要なファイル・対象箇所・行範囲を絞る。
- JSON、CSV、ログ、生成物、依存関係ファイルなどの巨大ファイルは、全文表示せず、スクリプトやコマンドで必要なキー・行・集計情報だけを抽出する。
- 全体構造の把握が必要な場合も、見出し、定義、関数名、キー一覧などを先に抽出してから対象範囲を読む。
- 全文を読む必要があると判断した場合のみ、分割して作業に必要な範囲を読む。
- 一度取得した情報を理由なく何度も再読み込みしない。「念のため全文を読む」は避け、必要性を判断してから読む。

## 実装時の互換性と品質

- 既存の音量・ミュート設定とデータ引き継ぎ設定を維持する。保存キーや保存形式を変える場合は、既存利用者向けの移行と後方互換性を設計・検証する。
- iPhone Safari、Android Chrome、PC Chrome を考慮し、モバイルではノッチ、Dynamic Island、ホームインジケーターを避ける Safe Area に対応する。
- 画像は本来の縦横比を維持し、意図しない引き伸ばしや切り抜きを行わない。
- 低性能端末も想定して重いアニメーションや不要な常時処理を避ける。
- BGM/SE は開始・停止・画面遷移のライフサイクルを管理し、二重再生を防ぐ。ブラウザの自動再生制限も考慮する。
- React の再描画や StrictMode による effect の再実行を前提に、副作用、イベント登録、タイマー、通信、音声処理が重複しない実装にする。必要なクリーンアップを行う。
- ゲーム本体を変更するときは編集元と生成物を同期する。具体的なビルド方法とファイルの正本は `DEVELOPMENT.md` に従う。

## 検証と完了報告

- 変更箇所の確認だけでなく、関連する既存機能への回帰テストを行う。少なくとも `git diff --check` と、変更範囲に応じた `DEVELOPMENT.md` / `tools/README.md` 記載のチェックを実行する。
- 自動テスト、エミュレーション、ローカルブラウザ確認と実機確認を区別する。実機で未確認の項目を「確認済み」と報告しない。
- 技術的に実現できなかったこと、失敗したテスト、未確認事項を隠さず、成功したように報告しない。
- Codex の完了報告は、変更内容、テスト結果、コミット、未解決事項だけを日本語で簡潔に記載する。