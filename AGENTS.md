# リポジトリ共通の開発ルール

このファイルは、このリポジトリ全体で作業する AI エージェントに適用します。詳細な手順は
[`DEVELOPMENT.md`](DEVELOPMENT.md)、プロジェクトの前提は [`PROJECT_CONTEXT.md`](PROJECT_CONTEXT.md) を正本とします。

## 作業の開始と範囲

- 作業開始時に、この `AGENTS.md`、[`README.md`](README.md)、[`PROJECT_CONTEXT.md`](PROJECT_CONTEXT.md) を読む。続いて `DEVELOPMENT.md` と変更箇所に適用される下位の `AGENTS.md`、関連文書を確認する。
- `git status --short --branch` で既存変更を確認し、利用者の変更を破棄・上書きしない。
- 依頼範囲外のコード、仕様、データ、アセット、文書を「ついでに」変更しない。不明な要件は推測せず、質問するか未確認事項として明記する。
- ゲーム仕様・バランス、ランキング、Supabase のスキーマや通信、セーブデータ形式は、明示的な依頼と互換性・移行方針なしに変更しない。

## Codex クラウド運用

- 1つの開発依頼につき必ず新しい Codex チャットを開始し、そのチャット内で、remote の有無にかかわらず依頼された実装、ビルド、テスト、コミットまで完結する。push 可能な場合は push と PR 作成まで行う。
- 各チャットでは、作業開始前に GitHub remote、対象リポジトリ、現在のブランチ、作業ツリー、`main` との差分を確認する。remote が利用できない、または push できないことを理由に、実装前やコミット前で作業を止めない。
- remote を利用できる場合は最新情報を取得し、最新の `main` から依頼ごとの新しい作業ブランチを作成する。remote を利用できない場合も、利用可能なローカルの履歴を基に作業ブランチで作業を続ける。過去チャットのローカルブランチや未 push の変更は引き継がない。
- 変更がある場合は必ず `git commit` を実行し、実際のコミット SHA が存在することを完了条件とする。変更がない場合のみコミットを不要とし、「PR メタデータ作成」だけで完了扱いにしない。
- push 可能な場合はコミットを push して PR を作成する。push 不可能な場合や remote が一時的に利用できない場合も、ローカルコミットを完成成果物として残し、コミット SHA、作業ブランチ名、未 push である理由を完了報告に記載する。
- ユーザーが添付した画像やファイルは、そのチャットで受け取ったものだけを使用する。必要な画像が実行環境に存在しない場合は推測で実装せず、ユーザーに再添付を求める。
- すでに `main` へマージ済みで差分がない変更について、重複する PR を作成しない。一方、依頼内容が新しい変更であれば、過去のマージ済み PR を理由に完了扱いせず、新規差分として実装する。
- PR 作成時に 400 エラーが発生した場合は、`main` との差分の有無、コミットの push 状態、既存 PR の有無、head と base の指定を確認してから再実行する。

## 実装時の互換性と品質

- 既存の音量・ミュート設定とデータ引き継ぎ設定を維持する。保存キーや保存形式を変える場合は、既存利用者向けの移行と後方互換性を設計・検証する。
- iPhone Safari、Android Chrome、PC Chrome を考慮し、モバイルではノッチ、Dynamic Island、ホームインジケーターを避ける Safe Area に対応する。
- 画像は本来の縦横比を維持し、意図しない引き伸ばしや切り抜きを行わない。
- 低性能端末も想定して重いアニメーションや不要な常時処理を避ける。
- BGM/SE は開始・停止・画面遷移のライフサイクルを管理し、二重再生を防ぐ。ブラウザの自動再生制限も考慮する。
- React の再描画や StrictMode による effect の再実行を前提に、副作用、イベント登録、タイマー、通信、音声処理が重複しない実装にする。必要なクリーンアップを行う。
- ゲーム本体を変更するときは編集元と生成物を同期する。具体的なビルド方法とファイルの正本は `DEVELOPMENT.md` に従う。

## 検証と完了報告

- 変更箇所の確認だけでなく、関連する既存機能への回帰テストを行う。少なくとも `git diff --check` と、変更範囲に応じた `DEVELOPMENT.md` / `tools/README.md` 記載のチェックを実行する。
- 自動テスト、エミュレーション、ローカルブラウザ確認と実機確認を区別する。実機で未確認の項目を「確認済み」と報告しない。
- 技術的に実現できなかったこと、失敗したテスト、未確認事項を隠さず、成功したように報告しない。
- Codex の完了報告は、変更内容、テスト結果、push 結果、PR 番号、未解決事項だけを日本語で簡潔に記載する。PR を作成できなかった場合は、その旨を PR 番号の代わりに記載する。
